# OS / editor junk
.DS_Store
Thumbs.db
*.swp
*~

# Local-only data (personal data must never be committed to this public repo)
/data/
*.local.json
*.local.js

# Local import artifacts (not part of the published site)
*.zip

# Workflow-generated content staging (merged into game files, then removed)
_gen/

# bulk_migrate_images cleanup (2026)
zukan_cards/processed/*_L1_segmented.png
zukan_cards/_pipeline/

# Source ingestion artifacts (downloaded raw photos, not part of published site)
zukan_cards/_inbox/
zukan_cards/_archive/
