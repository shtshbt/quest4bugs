# kom_seisu 機械化可能性監査 (2026-08-28)

- 対象: `docs/komorebi_seisu_curriculum.md` v0.1 (整数の性質、kom_seisu)
- 方法: `docs/komorebi_mechanization_audit_2026_08_14.md` 1 章の 2 層ソルバー (T1 構造盲目、T2 語彙ルックアップ)、閾値 0.75、判定 3 区分、横断穴 C1 から C4
- T2 の運用注: `docs/komorebi_categories.md` 記載原則 6 の運用注 (恣意的な対応表にのみ T2 判定を適用する) を 1.2 章で Lv ごとに適用判断した
- 立場: 対象 doc の設計意図の説明は受け取っていない。11 章の自己見積りは反証対象として扱い、doc の記述だけから独立に計算した

## 0. 結論 3 行

1. 10 段すべてが閾値 0.75 を超え、うち 7 段が 0.90 以上である。最も安いのは Lv1 で、`div_of` の答えが「並んだ約数の列の最大値」であるため、1 文字も読まずに 1.00 で通る。11 章の自己見積り (Lv1 は 0.60) はここを見落としている。
2. 4.4 章の主装置は成立していない。場面 3 段 (Lv4、6、10) が実際に使う反転はいずれも `scene_lcm_capped` (反転 A) で、これは問題文に数を 3 つ持つ唯一の pattern であるため、大小語ソルバーに「数が 3 つなら上限つき公倍数」の 1 行を足すだけで無効になる。4.2 章が反転を「1 問以上」としか要求していないことと合わせると、規則に完全適合したまま Lv4 は T2 で 1.00 に達する。
3. 4.7 章の記号依存は数値照合ソルバーを落とさない。9.5 章の部品設計が 2 通りしかなく、数を含まない部品の位置が設計ごとに固定されているため、部品数で設計を見分けて並べるだけで整列は 1.00 になる。検証 21 が要求する「数なし部品の位置が末尾以外で一様」は、9.5 章の 2 設計では原理的に満たせない。

## 1. 判定表

値はすべて 5 問セットに対する期待正答率である。1 問単位の内訳は 1.3 章に置く。T1 と T2 の高いほうで判定した。

| Lv | T1 | T1 ソルバーの手続き | T2 | T2 ソルバーの手続き | 判定 |
|---|---:|---|---:|---|---|
| 1 | 1.00 | find_all は選択肢に載っていない唯一の数の約数を選び、normal は数が 1 つならその約数の個数、数が並んでいれば最大値を答える | 1.00 | 同左 (語彙に分岐する必要がない) | 機械化可能 |
| 2 | 0.80 | find_all は常に倍数側を選び (対比ペアで 1 問落とす)、normal は 40 以上の数があればその数 ÷ 小さい数の商、無ければ 2 数の積 | 1.00 | 「倍数」「約数」の 2 語で手続きを切り替える | 部分的に可能 (T1 による。1.2 章) |
| 3 | 0.80 | normal は常に最小公倍数、find_all は候補の最大値が 2 数の最大値以下なら公約数、超えていれば公倍数 | 1.00 | 「最大公約数」「最小公倍数」「公約数」「公倍数」の 4 語で分岐 | 部分的に可能 (T1 による。1.2 章) |
| 4 | 0.60 | 数が 3 つ現れたら上限以下で最大の公倍数、2 つなら常に最小公倍数 | 1.00 | 数が 3 つなら上限つき公倍数、それ以外は大なら最大公約数、小なら最小公倍数 | 機械化可能 |
| 5 | 0.85 | 整列は部品数で 9.5 章の 2 設計を判別し (5 部品なら数なし部品を先頭にして数値連鎖、4 部品なら問題文の 2 数の出現順に並べ数なし部品を末尾の 1 つ手前)、normal は数の個数で素数計数と (指数 + 1) の積を切り替え、診断は 4 択から 1 つ選ぶ | 0.95 | 同左 + 診断は答案の式の表層形を 8 章 W 表で引く | 機械化可能 |
| 6 | 0.50 | 単位が分か秒なら最小公倍数、それ以外も最小公倍数、診断は 4 択から 1 つ選ぶ | 0.90 | 「次に」「k 回目」は最小公倍数、数が 3 つなら上限つき公倍数、大小語は語の写像、診断は答案の式を W 表で引く | 機械化可能 (診断の扱いに依存。4 章) |
| 7 | 0.80 | find_all は (大きい数 − 小さい数) の約数のうち小さい数より大きいものを選び、formulation は引き算の肢を選び、normal は小さい数の個数と上限値の有無で 3 手続きを切り替える | 1.00 | 同左 + 「わり切れる」「あまる」「何個」で normal を分岐 | 部分的に可能 |
| 8 | 0.85 | formulation は選択肢集合に「商に 1 をたす」形があればそれ、無ければ「あまり」形を選び、normal は数が 1 つなら N ÷ 7 の商 + 1、2 つなら最小公倍数、診断は 4 択から 1 つ選ぶ | 0.95 | 同左 + 診断は答案が商を使っていれば `quotient_remainder` | 機械化可能 |
| 9 | 0.50 | find_all は 2 数の公約数、normal は常に最大公約数、診断は 4 択から 1 つ選ぶ | 0.95 | 選択肢に「正しい (べつのとき方)」があればそれ、無ければ答案の式を W 表で引く。normal は足場と問い文の語で分岐 | 機械化可能 |
| 10 | 0.73 | 整列は 9.5 章の非連鎖 1 設計に固定なので確実に取り、数が 3 つなら上限つき公倍数、find_all は候補の最大値で公約数側か公倍数側かを決め、formulation は常に最小公倍数、診断は 4 択から 1 つ選ぶ | 0.90 | 同左 + 大小語の写像と W 表 | 機械化可能 |

閾値超えは 10 段中 10 段。0.90 以上 (機械化可能) が Lv1、4、5、6、8、9、10 の 7 段、0.75 から 0.89 (部分的に可能) が Lv2、3、7 の 3 段。0.74 以下の段は無い。

### 1.1 doc の自己見積り (11 章) との差

| Lv | doc の T1 | 本監査の T1 | 差の出どころ |
|---|---:|---:|---|
| 1 | 0.60 | 1.00 | `div_of` の答えが列の最大値であることを見落としている。12 章の未確定 2 は `div_of` を分散装置として置いたと書くが、実際には 28 pattern 中もっとも安い |
| 2 | 0.60 | 0.80 | normal 2 問を数の個数と大小で分けられる。数の個数は監査 1.1 章が T1 に許した手がかりである |
| 3 | 0.40 | 0.80 | find_all 2 問が候補の帯で判別できる。公約数は min(a, b) 以下、公倍数は lcm(a, b) 以上なので帯が重ならない |
| 5 | 0.25 | 0.85 | 整列 2 問を 0.25 と見積もっているが、9.5 章の設計が 2 通りしか無いため部品数で判別できる |
| 8 | 0.25 | 0.85 | 対比ペアの 2 問の選択肢集合が異なるため、選択肢の形だけで問い分けが決まる |
| 10 | 0.25 | 0.73 | 整列 1 問が固定設計で無償、`scene_lcm_capped` が数の個数で無償 |

### 1.2 運用注の適用判断

原則 6 の運用注は「恣意的な対応表 (語から演算への暗記が構造理解を代替するもの) にのみ T2 判定を適用する。量名や単位の定義、問い文が名指しする量の読み取りは学習内容そのものであり、それによる高スコアは不合格としない」と読める。適用に迷った箇所を、適用した場合としない場合の両方の数値とともに示す。

| 箇所 | 適用する場合 | 適用しない場合 | 判断 | 理由 |
|---|---:|---:|---|---|
| Lv2 の「倍数」「約数」 | T2 を不問にし、判定は T1 0.80 | T2 1.00 で機械化可能 | 適用する | 「12 の倍数をえらべ」は問い文が名指しする量の読み取りそのもの。5.1 章がこの段を弾層と定義し、向きの判別を求めていない。ただし T1 が 0.80 なので判定は変わらない |
| Lv3 の 4 語 | T2 を不問にし、判定は T1 0.80 | T2 1.00 で機械化可能 | 適用する | 同上。判定は T1 で決まる |
| Lv7 の「わり切れる」「あまる」 | T2 を不問にし、判定は T1 0.80 | T2 1.00 で機械化可能 | 適用する | わり算の定義の読み取りに当たる。ただし本 Lv の学習内容である「割る数を求めるなら引く」を選択肢の形だけで取る手続きは T1 に属し、そちらが 0.80 を作っている |
| `next` と `nth` から最小公倍数への写像 (Lv6、8、10) | 該当箇所を 0.5 に落とすと Lv6 の T2 は 0.80 | Lv6 の T2 は 0.90 | 適用しない | 3.4 章自身が「`next` と `nth` は公倍数側にしか実体が無い」と書いており、語から演算への 1 対 1 写像として暗記できる。4.4 章が `max` と `min` についてだけ写像を壊しにいっていること自体が、`next` と `nth` の写像が生きている証拠である。量名でも単位の定義でもない |
| 診断ラベル (答案の式の表層形から W 表を引く) | 診断を一様推測 0.25 とすると Lv5 0.85、Lv6 0.70、Lv8 0.85、Lv9 0.87、Lv10 0.83 | 上表のとおり Lv5 0.95、Lv6 0.90、Lv8 0.95、Lv9 0.95、Lv10 0.90 | 適用しない | 誤りを型で呼べることは学習内容だが、ソルバーが使うのは「答案に × があれば `not_minimal`」「答案が商を使っていれば `quotient_remainder`」という式の表層形の照合であり、場面の向きを一度も読んでいない。ただしこの判断が判定を動かすのは Lv6 だけである (適用すると Lv6 のみ 0.70 の耐性あり) |

### 1.3 値の内訳

セットは 6 章の Lv 別詳細に書かれた 5 問構成を用い、4.2 章の最低要件だけを満たす最悪構成が別にある場合は併記する。

Lv1 (T1 1.00): 例 1 `div_count` は数が 1 つなので約数を数えて可、例 2 と例 3 相当の `div_of` 2 問は列の最大値を書くだけで可、`div_select` 2 問は選択肢に載っていない数の約数を選ぶだけで可。5 問すべて。足場は `scaffold` が別 field なので `text` だけを読むソルバーの邪魔をしない。

Lv2 (T1 0.80): find_all 3 問は `mul_select` 2 と `div_select` 1 で、常に倍数側を選ぶと 2 問。normal 2 問は `mul_count` (40 以上の数を持つ) と `mul_nth` (小さい数 2 つ) で、数の大小で分けて 2 問。合計 4 問。

Lv3 (T1 0.80): normal 3 問は `gcd_num` 1 と `lcm_num` 2 で、常に最小公倍数を答えると 2 問。find_all 2 問は候補の帯で判別して 2 問。合計 4 問。5 問目の pattern を抽選する実装では 0.70 から 0.80 のあいだ、期待 0.75。

Lv4 (T1 0.60、T2 1.00): T1 は `scene_lcm_capped` を数の個数で取り、`scene_lcm_size` 2 問を常時最小公倍数で取り、gcd 側 2 問を落とす。T2 は 6 章の例示セット (反転 2 問) に対しては 0.80、4.2 章の最低要件だけを満たす最悪構成 (反転 1 問で、それが反転 A) に対しては 1.00。詳細は 2.3 章。

Lv5 (T1 0.85): 整列 2 問はいずれも 9.5 章の設計そのままなので 2 問、normal 2 問 (`prime_check` は数 2 つ、`factor_count` は素因数分解の式を含み数が 3 つ以上) は数の個数で分けて 2 問、診断 1 問は 0.25。合計 4.25 問。

Lv6 (T1 0.50): normal `scene_lcm_time` 1 問と formulation `scene_lcm_capped` 1 問 (正解肢が「最小公倍数」) を常時最小公倍数で取り、normal `scene_gcd_count` 1 問を落とし、診断 2 問は 0.25 ずつ。合計 2.5 問。4.2 章の最低要件は gcd 側 1 問以上なので、その 1 問が診断に当たる構成では非診断 3 問がすべて lcm 側になり、常時最小公倍数ソルバーは 0.70 に届く。

Lv7 (T1 0.80): find_all 1 問と formulation 1 問は確定で取り (2.4 章)、normal 3 問のうち 2 問を数の構成で判別。合計 4 問。3 手続きが完全に分離する実装では 1.00。

Lv8 (T1 0.85): formulation 2 問は選択肢集合の形で確定 (2.4 章)、normal 2 問は数の個数で確定、診断 1 問は 0.25。合計 4.25 問。

Lv9 (T1 0.50): find_all 1 問は確定、normal 2 問は常時最大公約数で 1 問、診断 2 問は 0.25 ずつ。合計 2.5 問。T2 では診断 2 問のうち 1 問が `correct_alternative` と 4.2 章が定めているため、その肢の実在で 1 問が確定する (2.5 章)。

Lv10 (T1 0.73): 整列 1 問と `scene_lcm_capped` 1 問は確定、find_all 1 問は候補の帯で 0.9、formulation 1 問は常時最小公倍数で 0.5、診断 1 問は 0.25。合計 3.65 問。

## 2. 横断穴

### 2.1 C1 から C4 に当てはまるもの

C1 (整列の一本鎖規則がそのままソルバーになる)。対処が宣言されているが成立していない。4.7 章は一意性の根拠を `requires` と `produces` の位相順序に置き、数を含まない部品をちょうど 1 つ混ぜると書く。しかし 9.5 章が与える部品設計は 2 通りしかなく、数なし部品の位置は設計ごとに固定されている (`factorize` では先頭、`gcd_by_factor` では 3 番目)。部品数 (5 か 4) は形式から見えるので、ソルバーは設計を判別してから数値連鎖または問題文との数の照合で並べればよい。さらに `gcd_by_factor` の非連鎖形は、問題文が「48 と 60 の最大公約数を」と 2 数をこの順で名指しし、先頭 2 部品がその順で並ぶ。数値の照合だけで正順が決まる。Lv10 は 9.5 章により非連鎖形 1 設計に固定されるため、整列枠の patternId は定数である。

C2 (正解選択肢の位置分散が無規定)。選択肢については塞がれている。4.6 章が注入 `random` でのシャッフルを明記し、検証 22 が位置分布の一様性 (各位置 20 から 30%) を要求している。ただし残余が 2 つある。第一に、整列の部品の初期提示順に規定が無い。検証 20 と 21 は位相順序と数なし部品の位置を検査するが、提示順が正順の固定置換になっていないことは誰も検査しない。第二に、find_all の候補の並び順に規定が無い (6 章の例はすべて昇順)。

C3 (一意性のための固定語彙が lookup table になる)。3 か所で開いている。第一に、3.4 章の `next` と `nth` は公倍数側にしか実体が無いと doc 自身が書いており、語から演算への 1 対 1 写像として完全に働く。第二に、3.3 章の T4 (バス、電車、ライト) は公倍数側専用で、単位が分か秒であることから向きが決まる。単位記号の見た目は監査 1.1 章が T1 に許した手がかりであり、これは T2 ですらない。第三に、8 章の W 表と検証 16 (「`errorType` の誤りが答案に実在し、他の誤りが混入していない」) が、答案の式の表層形から誤りラベルへの一意写像を要求している。品質を保証するために置いた規則が、そのままラベル同定の手続きになっている。

C4 (セット内の型分散規則が無い)。4 本の中で最もよく塞がれている。4.2 章が全 10 段の配合列を持ち、9.4 章が空間サイズを固定し、検証 5 と 10 と 11 が突き合わせる。指摘は 2 点だけである。第一に、配合が Lv ごとに固定で公開されているため、ソルバーは事前にセットの型構成を知っている (Lv1 は `div_count` 1、`div_of` 2、`div_select` 2 の固定配分と書かれている)。第二に、分散を形式ごとに割り当てると形式が型を漏らす。2.6 章に分けて書く。

### 2.2 カテゴリ固有の穴 (概要)

| # | 穴 | 影響する Lv |
|---|---|---|
| S1 | 反転 A (`scene_lcm_capped`) が 3 数を持つ唯一の pattern であり、読まずに識別できるので反転として働かない | 4、6、10 |
| S2 | 4.2 章の「大小語の反転 1 問以上」では検証 30 (大小語ソルバー 0.70 未満) を満たせない | 4、6、10 |
| S3 | `div_of` の答えが列の最大値である | 1 |
| S4 | 対比ペアが数の多重集合の一致しか要求せず、選択肢集合と答えの型の一致を要求していない | 8、9 |
| S5 | 診断は答案の式の表層形から一意にラベルが決まるよう検証 16 が要求している | 5、6、8、9、10 |
| S6 | 4.2 章が Lv9 の診断 2 問のうち 1 問を `correct_alternative` と定めるため、その肢の実在が答えを教える | 9 |
| S7 | 足場が向きを名指しする実例のとき、本文を読まずに向きが決まる | 7、9 |
| S8 | 形式が Lv ごとに固定で UI から既知なので、形式が pattern を漏らす | 5、7、9、10 |
| S9 | 6 章の例示セットが doc 自身の規則に違反しており、実在証明が既に破れている | 6、7 |
| S10 | 検証 21 (数なし部品の位置が一様) が 9.5 章の 2 設計では原理的に満たせない | 5、10 |

### 2.3 S1 と S2 の詳細 (4.4 章の主張の検査)

4.4 章は、問い語と向きの対応が一致する問題と反転する問題を各 1 問以上含めることで、セット内で大小語から向きへの写像が関数として定義できなくなると主張する。混入率を 4.2 章の配合列から読むと次のようになる。

| Lv | 4.2 章の要求 | 6 章の例示セットでの反転数 | 大小語を持つ問題数 | 純粋な大小語ソルバーの値 |
|---|---|---:|---:|---:|
| 4 | 大小語の反転 1 問以上 | 2 (例 4 が反転 B、例 5 が反転 A) | 5 | 0.60 (例示セット) / 0.80 (規則下限) |
| 6 | 大小語の反転 1 問以上 | 1 (例 5 が反転 A) | 2 | 0.50 (残り 3 問は別手続きが要る) |
| 10 | 大小語の反転 1 問以上 | 1 (例 3 が反転 A) | 2 | 0.60 (同上) |

規則の下限は反転 1 問である。5 問すべてが大小語を持つ Lv4 では、反転 1 問の構成に対して大小語ソルバーは 4 問正答し 0.80 となる。閾値 0.75 を超えるだけでなく、doc 自身が検証 30 で設定した基準 (Lv4、6、10 の 200 セットに対し 0.70 未満) も満たせない。4.2 章の配合列と検証 30 が同時に成立しない。規則を満たした生成器が自分の test に落ちる状態である。

さらに重い問題は反転の実体である。反転 A は `scene_lcm_capped` で実現されており、この pattern は 9.2 章により上限 C を持つ。したがって問題文に現れる数が 3 つになる。Lv4、6、10 の他のどの pattern も 2 数である。数の個数は監査 1.1 章が T1 に明示的に許した手がかりであるから、ソルバーは「数が 3 つなら 2 数の最小公倍数を求め、上限以下で最大の倍数を答える」という 1 行を持てばよい。この 1 行は問題文を一切読まない。したがって反転 A は大小語ソルバーに対する反例として機能しない。

6 章の例示セットに対して、この 1 行を足した大小語ソルバーを走らせると次になる。

- Lv4: 例 1 (max 一致) 正、例 2 (min 一致) 正、例 3 (min 一致) 正、例 4 (min 反転 B) 誤、例 5 (max 反転 A) 正 = 0.80
- Lv6: 例 3 (max 一致) 正、例 5 (max 反転 A) 正、例 2 (next) 正、診断 2 問は 0.25 ずつ = 0.70。診断に W 表を使うと 1.00
- Lv10: 例 2 (min 一致) 正、例 3 (max 反転 A) 正、例 1 (count、候補の帯で公約数側と判別) 正、例 4 (整列、固定設計) 正、例 5 (診断) 0.25 = 0.85

規則下限の構成、すなわち反転を 1 問だけ置き、それを反転 A にした Lv4 のセットでは、この解法は 5 問すべてを取る。例として gcd 側に `scene_gcd_size` (max 一致) と `scene_gcd_count` (max 一致)、lcm 側に `scene_lcm_size` (min 一致) 2 問と `scene_lcm_capped` (max 反転 A) を置く構成は、4.2 章の gcd 側 2 問以上、lcm 側 2 問以上、対比ペア 1 組、反転 1 問以上、9.4 章の上限 2 をすべて満たし、検証 7 (一致と反転が各 1 問以上) も満たす。T2 は 1.00 である。

結論として、4.4 章の主張は成立していない。真に写像を壊しているのは反転 B (`scene_gcd_count` の `ask: min`) だけであり、それを義務づける行が 4.2 章に無い。

### 2.4 S4 の詳細 (対比ペアが選択肢と答えの型で割れる)

4.3 章の対比ペアの定義は「問題文に現れる数がすべて同一で、問いの種類または向きだけが異なり、かつ答えが異なる 2 問の組」である。要求しているのは数の多重集合の一致と答えの相異だけで、選択肢集合の一致も答えの型の一致も要求していない。

Lv8 の P5 ペア (6 章の例 1 と例 2) で破れる。例 1 の選択肢は「100 ÷ 7 のあまり」「100 ÷ 7 の商」「100 − 7」「100 × 7」、例 2 は「100 ÷ 7 の商に 1 をたす」「100 ÷ 7 の商」「100 ÷ 7 のあまり」「100 ÷ 7 の商とあまりをたす」である。集合が異なるので、「選択肢集合に商に 1 をたす形があればそれを選び、無ければあまりの形を選ぶ」だけで両方取れる。選択肢の形状は監査 1.1 章が T1 に許した手がかりである。

ペアが normal どうしになった場合も同じ結論になる。`cycle_weekday` の答えは曜日名、`cycle_count` の答えは個数であり、答えの型が違う。問題を読まなくても、答えの入力欄が曜日の選択か数値かで問い分けが決まる。

Lv4 の P3 ペア (例 1 と例 2) は選択肢の顔ぶれまで同一に作られており、この穴は無い。ペアの品質が型によって揃っていない。

Lv7 の formulation にも同種の穴がある。6 章の例 3 の選択肢は 35 − 3、35 + 3、35、35 × 3 で、演算子がすべて異なる。「引き算の肢を選ぶ」だけで正答する。これは検証 15 (誤答の少なくとも 1 つは正解と同じ演算子の並びを持つ) にも違反しており、doc の例題が doc の検証項目を満たしていない。

### 2.5 S5 と S6 の詳細 (診断ラベル)

Lv ごとの可用ラベルの組と、そこから作れる 3 肢の組み合わせ数を数え直した。

| Lv | 誤りラベル数 | 正答肢 | 4 肢の顔ぶれの通り数 (C(誤りラベル数, 3)) | 一様推測 |
|---|---:|---|---:|---:|
| 5 | 3 | `correct` | 1 | 0.25 |
| 6 | 5 | `correct` | 10 | 0.25 |
| 8 | 4 | `correct` | 4 | 0.25 |
| 9 | 5 | `correct` または `correct_alternative` | 10 | 0.25 |
| 10 | 8 | `correct` または `correct_alternative` | 56 | 0.25 |

Lv5 は可用ラベルがちょうど 3 種であるため、4 肢の顔ぶれが完全に固定される。8.1 章は「誤りラベルが 3 種を切ると顔ぶれが固定して選択肢から正解が読めてしまう」と書くが、3 種ちょうどでも顔ぶれは固定する。ただし 4.6 章のシャッフルにより位置は読めないため、顔ぶれの固定それ自体は 0.25 を超えさせない。

ラベル名からの推測は 3 か所で成立する。

第一に `word_cue` (大小のことばにつられている) は、8 章が「`swap_gcd_lcm` との区別は答案に添えた理由による」と定めているのに対し、6 章のすべての答案は「しき」と「こたえ」しか持たず理由を書いていない。この書式のままでは `word_cue` は正解になりえず、常に誤答肢である。除外できると Lv6 と Lv9 の推測は 0.25 から 0.33 に上がる。

第二に `calc_only` (計算だけまちがえている) も 6 章のどの例でも正解になっていない。正解になる答案 (向きも手順も正しくわり算だけを誤る) を生成する規定は 4.8 章にも 9 章にも無い。実際に使われないなら、Lv5 の実効的な答えの空間は `correct`、`count_off`、`factor_incomplete` の 3 つに縮み、推測は 0.33 になる。

第三が最も重い。4.2 章の Lv9 の最低要件は「診断 2 問の `errorType` が互いに異なり、うち 1 問は `correct_alternative`」である。毎セット必ず 1 問の答えが `correct_alternative` だと決まっている。4.8 章は「正答肢は `correct` または `correct_alternative` のどちらか一方」と書くだけで、答えが誤りラベルである問題の正答肢としてどちらを置くかを規定していない。6 章の Lv9 の例では、答えが `correct_alternative` の問題 (例 2) だけが「正しい (べつのとき方)」を持ち、もう一方 (例 4) は「正しい」を持つ。したがって「選択肢に正しい (べつのとき方) があればそれを選ぶ」で 1 問が確定する。残り 1 問も「正しい」を除いた 3 肢からの選択に縮む。診断 2 問の期待は 0.25 ずつの 0.5 から 1.33 に上がる。

正解ラベルの分布については、4.8 章が正答案の比率を Lv ごとのプール全体で 20 から 30% と定めるだけで、誤りラベル間の分布に規定が無い。特定のラベルに寄った実装 (例えば Lv5 が `count_off` に寄る) は検証項目のどれにも引っかからない。この点は 4 章に限界として書く。

### 2.6 S7 と S8 の詳細

足場の向き漏洩 (S7)。4.1 章は足場を「小さい数の実例」に統一すると書くが、実例が向きに中立であることを要求していない。6 章の Lv7 の例 1 の足場は「5 でも 7 でもわり切れるいちばん小さい整数は 35 です」で、公倍数側の手続きを名指ししている。Lv9 の例 1 の足場は「12 と 18 の公約数は 1, 2, 3, 6 です」で、公約数側を名指ししている。どちらも本問の向きと一致するため、足場だけを読めば向きが決まる。4.1 章の Lv4 の例 (公約数と公倍数を両方書く足場) はこの穴を持たない。足場は各セットの前半 2 問に必ず付く (検証 24) ので、漏洩は毎セット 2 問まで及ぶ。

形式が型を漏らす (S8)。5.2 章は形式配合を Lv ごとに固定し、形式は UI から既知である。その結果、形式から pattern への写像が単射に近い段がある。Lv7 の find_all は 1 問だけで、6 章では `rem_divisor` に割り当てられている。Lv9 の find_all も 1 問で `common_div_select` である。Lv10 の ordering は 9.5 章により `gcd_by_factor` の非連鎖形に固定である。Lv5 の diagnosis は 1 問で `factor_count` である。9.4 章の空間サイズは形式配合を無視して pattern を数えているため、形式枠ごとの実効空間は表の値より小さい。C4 対策として置いた型分散が、形式単位では分散になっていない。

### 2.7 S9 の詳細 (doc 内の自己矛盾)

監査の対象外だが、実在証明 (Phase 4) の前に落ちる箇所なので列挙する。

1. 4.2 章の Lv6 は「T4 は 2 問以上使わない」と定め、検証 8 も同文である。しかし 6 章の Lv6 の例示セットは例 1 (バス)、例 2 (電車)、例 4 (ライト) の 3 問が T4 である。
2. 検証 21 は「全生成にわたるその部品の位置が末尾以外で一様」を要求する。9.5 章の部品設計は 2 通りで、数なし部品の位置は `factorize` で先頭、`gcd_by_factor` で 3 番目に固定される。4 部品と 5 部品にまたがる位置分布が一様になることはない。4.7 章の「その部品の位置は注入 `random` により決める」と、同章の「位相順序がちょうど 1 通り」も両立しない。位相順序が一意なら位置は決まっており、乱数の余地が無い。
3. 検証 15 は formulation の誤答の少なくとも 1 つが正解と同じ演算子の並びを持つことを要求するが、6 章の Lv7 の例 3 は 4 肢の演算子がすべて異なる。
4. 9.2 章は remainder 領域の p と q を互いに素と定めるが、6 章の Lv7 の例 2 は 4 と 6 を使っている。
5. 9.2 章は find_all の誤答候補に「もう一方の向きの集合に属する数」を 1 個以上含めると定めるが、6 章の Lv1 の例 3 の解説は「候補の中に 24 の倍数が無い」と自ら書いている。Lv3 の例 4 (6 と 8 の公倍数を選ぶ問題) の候補にも公約数は含まれていない。

### 2.8 patternId 空間と上限規則の突き合わせ (検証 5 の再計算)

4.2 章の上限 `ceil(5 ÷ 空間サイズ)` について、鳩の巣原理で破れる Lv は無い。9.4 章の主張は正しい。各段を計算し直した結果を示す。

| Lv | 空間 | 上限 | 4.2 章の最低要件が要求する最小の型数 | 破れるか |
|---|---:|---:|---|---|
| 1 | 3 | 2 | 固定配分 (2, 2, 1) で最大値 2 | 破れない (余地ゼロ) |
| 2 | 4 | 2 | 4 種すべて。5 問なので 1 種が 2 回 | 破れない |
| 3 | 4 | 2 | 4 種すべて | 破れない |
| 4 | 4 | 2 | gcd 側 2 種以上と lcm 側 2 種以上 | 破れない |
| 5 | 4 | 2 | 4 種すべて | 破れない |
| 6 | 7 | 1 | 5 問すべて相異 (連鎖の 2 問が同型なら 4 種) | 破れない |
| 7 | 4 | 2 | 約数側 1 問以上と倍数側 1 問以上 | 破れない |
| 8 | 4 | 2 | ペア 2 種と `scene_lcm_time` | 破れない |
| 9 | 4 | 2 | ペア 2 種 | 破れない |
| 10 | 26 | 1 | 5 問すべて相異 | 破れない |

ただし 2 点を付す。第一に Lv1 は固定配分の最大値が上限とちょうど一致しており、余地がゼロである。配分を動かす提案 (12 章の未確定 2) は上限規則と同時に検討する必要がある。第二に Lv10 の空間 26 は形式配合と両立しない。5 枠のうち ordering 枠は 9.5 章により `gcd_by_factor` の 1 種に固定されるので、空間 26 が効くのは残り 4 枠だけである。検証 11 (各 Lv で生成しうる patternId の異なり数が空間サイズと一致する) は、ordering 枠が定数である以上、残り 4 枠で 26 種を回すことによってしか満たせない。

隣接相異 (4.2 章) についても破れは無い。Lv1 の (2, 2, 1) は ABABC で満たせる。

## 3. 最小修正案

各行の前半が閾値を下回らせる最小の手、後半が 1 章の弱点 (場面から向きを数量関係で判断する) まで満たす案である。前半だけでは目的を満たさない場合がある。

| Lv | 最小修正 (閾値を下回らせる) | 目的まで満たす案 |
|---|---|---|
| 1 | `div_of` を「約数の列から 1 つだけ欠けている数を答える」形に変え、`div_select` の候補に N の倍数を必ず 1 個入れて N を最大値でなくする | Lv2 の P1 ペア (同一 N、同一候補で約数側と倍数側) を Lv1 の find_all 2 問に前倒しし、5 問中 2 問を単一手続きで取れなくする |
| 2 | `mul_nth` の問題文に範囲の見た目を与えて (1 から数えて k 番目) `mul_count` との数の個数の差を消す | normal 2 問も対比ペアにし、同一の数で個数と順番を問い分ける。find_all のペアと合わせてセット内に 2 組置く |
| 3 | find_all 2 問を同一候補、同一 2 数の対比ペアにし、候補に公約数と公倍数を混在させて帯による判別を潰す | 上記に加え、5 問目の pattern を `gcd_num` か `lcm_num` に固定せず、normal の対比ペアと find_all の対比ペアの 2 組体制にする |
| 4 | 4.2 章の要件を「大小語の反転 2 問以上、うち 1 問は反転 B (`scene_gcd_count` の `ask: min`)」に変える。これで大小語ソルバーは 0.60 に落ちる | あわせて数の個数による反転 A の識別を潰す。上限 C を数として書かず場面の語で与える (1 辺が 1m の板におさまる)、または他の pattern にも第 3 の数を持たせて 3 数を反転 A の signature でなくする |
| 5 | 9.5 章の部品設計を 4 通り以上に増やし、数なし部品の位置を設計間で変える。あわせて問題文の 2 数の出現順と部品の順を独立に抽選する | 整列 2 問のうち 1 問を、部品に数を一切含まない形にする (johou_seiri Lv7 が 5 本中最良の耐性を示した形) |
| 6 | 診断 2 問のうち 1 問を、答案の式の表層形が同じで場面の向きだけが違う 2 通りから抽選する (同じ最大公約数の答案が gcd 場面では `correct`、lcm 場面では `swap_gcd_lcm` になる) | あわせて T4 を実際に 1 問以下に制限し (6 章の例示セットを直す)、単位が分か秒であることから向きが決まる経路を塞ぐ |
| 7 | formulation の誤答 3 つを正解と同じ演算子で作る (35 − 3、35 − 1、32 − 3 のような形) | 足場を両向き併記に変え (公倍数の実例と公約数の実例を並べる)、find_all を `rem_divisor` 以外の pattern でも作れるようにして形式から型への写像を単射でなくする |
| 8 | 対比ペアの 2 問の選択肢集合を完全に一致させる (4 肢を あまり、商、商に 1 をたす、商とあまりをたす で共通化する) | 4.3 章の定義に「選択肢集合と答えの型も一致させる」を追加し、P5 以外のペア型にも適用する |
| 9 | `correct` と `correct_alternative` のどちらを正答肢に置くかを注入 `random` で決め、答えが誤りラベルの問題でも `correct_alternative` を誤答肢に使う | あわせて normal 2 問の `gcd_num` と `lcm_num` を、足場が向きを名指ししない形にする |
| 10 | 9.5 章に非連鎖の部品設計をもう 1 つ以上足し、Lv10 の ordering を 2 設計以上から抽選する | 反転を反転 B に替えるか、Lv4 と同じく反転 2 問以上を要求する |

横断の修正としては次の 3 点が Lv 別の修正より先である。

1. 4.2 章の反転要件を「2 問以上」にし、少なくとも 1 問を反転 B とする。検証 30 の基準 (0.70 未満) と規則を同時に成立させる。
2. 検証 32 (数値照合整列ソルバー 0.35 未満) の敵ソルバーの定義に、部品数から設計を判別する前処理と、問題文に現れる数との照合を加える。現在の定義 (部品に現れる数を照合して鎖を作り、鎖に載らない部品の位置を等確率で選ぶ) は 9.5 章の 2 設計を落とせない。
3. 検証 30 の大小語ソルバーの定義に、数の個数による前処理を加える。現在の定義では反転 A を無償で取る解法を検出できない。

## 4. 監査の限界

doc だけでは判定できなかった項目を挙げる。

1. 診断の誤りラベルの出現分布。4.8 章は正答案の比率 (20 から 30%) だけを定め、誤りラベル間の分布を定めていない。一様なら診断は 0.25、偏っていればそれ以上になる。本監査は一様と仮定した。この仮定は Lv5、6、8、9、10 の値に効く。
2. 答案の式の表層形から誤りラベルを引く解法を T2 として数えるかどうか。1.2 章のとおり数えたが、数えない場合は Lv6 が 0.70 の耐性ありに下がる。10 段のうち判定が動くのはこの 1 段だけである。
3. 整列の部品の初期提示順。doc に規定が無い。正順の固定置換で提示する実装なら整列は無条件に 1.00、真にシャッフルするなら 1 章の値になる。
4. 6 章の例示セットと 4.2 章の規則下限の乖離。生成器がどちらに寄るかで値が 0.2 程度動く。本監査は Lv4 の T2 だけ規則下限を主値とし (2.3 章で最悪構成を明示できたため)、他は例示セットを主値とした。
5. 問い回しの表。7.2 章の 5 は「言い回しの表は開いた集合とし、生成器の内部データに置く」と書き、doc には現行分しか示されていない。C3 の残存量 (語彙表がどれだけ小さいか) を測れない。
6. 形式が UI から既知であるという前提の強さ。監査 1.1 章は形式を T1 の手がかりに含めているが、出題画面が形式のどこまでを事前に見せるかは `komorebi/app.js` の話であり、doc の範囲外である。本監査は形式と答えの入力型 (数値か曜日か集合か) が見えるものとして計算した。
7. `calc_only` と `word_cue` が実際に正解ラベルとして生成されるか。6 章のどの例でも正解になっていないが、生成しないと定めた行も無い。生成されなければ診断の実効空間は 1 段小さくなる。
8. Lv7 の normal 3 問の判別可能性。9.2 章のパラメタ帯からは 3 手続きが分離しうると読めるが、問題文の書き方 (「2 けた」のような語が数を持ち込むか) が確定しないため、2 問分離を主値とし 3 問分離を上限とした。T1 は 0.80 から 1.00 の幅を持つ。
9. 実子の誤答分布。本監査は正答率の上限を計算しただけで、装置が学習効果を持つかどうかは測っていない。

## 5. 再監査 (v0.2 対象)

- 対象: `docs/komorebi_seisu_curriculum.md` v0.2 (1489 行、2026-08-28)
- 0 章から 4 章は v0.1 に対する記録である。本章以降が v0.2 に対する独立判定であり、v0.1 の判定は本章では引用のためにのみ使う
- 設計側の主張: 閾値超え 0 段、T1 は 0.28 から 0.60、T2 は Lv4 以降が 0.35 から 0.60 (v0.2 の 11 章)

### 5.0 結論 3 行

1. 閾値超えは 10 段中 6 段 (Lv4、5、6、8、9、10) で、いずれも T2 が 0.90 以上である。v0.1 の 10 段中 10 段、うち 7 段が 0.90 以上という状態からは大きく改善しているが、閾値超え 0 段という主張は成立しない。T1 も全 10 段で設計側の見積りより高く、差の中央値は 0.20 である。
2. S1 の修正が形だけである。反転は数学的に上限または下限を要するため、反転問題は必ず与件を 3 数持ち、一致問題は必ず 2 数である。したがって「与件が 3 数なら大小語の写像を反転する」という 1 行で写像が復元され、場面 3 段の T2 は 1.00 に戻る。3.1 章に与件の数の列を足して gcd 側にも 3 数を持たせたことは、この 1 行を止めていない。検証 35 の敵ソルバーが v0.1 の攻撃 (3 数なら上限つき公倍数) で固定されているため、この攻撃はテストを素通りする。
3. 新設要素が新しい手がかりを 7 件作った。最も重いのは、対比ペアを全 10 段へ広げながらペア内の出題順を無規定のままにしたことで、順序が固定なら弾層 3 段が 0.80 から 1.00 に戻る。次が数ゼロ設計 (D6、D7) で、部品にパラメタが 1 つも無いため正順が定数になり、1 回覚えれば毎回取れる。3 番目が答案の「わけ」欄で、`word_cue` と `correct_alternative` に一意の表層マーカーを与えている。

### 5.1 v0.2 判定表

値は 5 問セットに対する期待正答率である。1 問単位の内訳は 5.1.1 章に置く。判定は T1 と T2 の高いほうで行い、Lv1 から Lv3 と Lv7 の T2 には運用注を適用した (5.2 章)。

| Lv | T1 | T2 | 判定 | ソルバーの手続き (1 行) | 設計側の値との差の出どころ |
|---|---:|---:|---|---|---|
| 1 | 0.60 | 1.00 | 耐性あり (運用注) | 列を持たない normal は約数を数え、列を持つ 2 問は片方の読み方に固定し、find_all は約数側に固定する | 差なし (設計側 0.60) |
| 2 | 0.60 | 1.00 | 耐性あり (運用注) | 与件の最大値が 40 以上なら上限系の 2 種、15 以下なら k 番目、find_all は倍数側に固定 | 設計側 0.40。与件の個数は揃ったが帯が分離しており、`mul_nth` (k は 5 から 15) が `mul_count` と `mul_capped` (上限は 40 から 120) から確実に切り出せる |
| 3 | 0.40 | 1.00 | 耐性あり (運用注) | normal は 3 種のうち 1 つに固定、find_all は公約数側に固定 | 差なし (設計側 0.40) |
| 4 | 0.67 | 1.00 | 機械化可能 | T1 は与件 3 数で第 3 数が 2 数より大きければ上限つき公倍数、小さくて単位が秒なら公倍数の k 倍、小さくて単位が こ なら公約数側。T2 はこれに「与件 2 数なら大小語の素直な写像、3 数なら反転」を足す | 設計側 T1 0.40 / T2 0.60。反転が必ず 3 数を持ち一致が必ず 2 数であることを使うと写像が復元される (5.4 章 H2) |
| 5 | 0.64 | 0.90 | 機械化可能 | 整列は数ゼロ設計なら記憶した定数列、数あり設計なら数値連鎖または素数の昇順。normal は列の積が先頭の数に等しいかで 2 種を分け、診断は 4 択から 1 つ | 設計側 T1 0.28 / T2 0.35。数ゼロ設計にパラメタが無く正順が定数であること (H3)、および数あり 5 設計がいずれも数値順で並ぶこと |
| 6 | 0.52 | 0.95 | 機械化可能 | Lv4 と同じ手続きに、診断は「わけ」欄が大小語を引用していれば `word_cue`、答案の式の向きが手続きの決めた向きと違えば `swap_gcd_lcm` を足す | 設計側 T1 0.30 / T2 0.60。H2 と、「わけ」欄が `word_cue` の一意マーカーになったこと (H4) |
| 7 | 0.66 | 1.00 | 耐性あり (運用注) | normal は 3 数がすべて 12 以下なら余りの等しい数、50 以上を含むなら残り 2 種のどちらかに固定。find_all は与件の帯で 3 種を分け、立式は引き算の 2 肢から選ぶ | 設計側 T1 0.37。与件の個数は 3 に揃ったが `rem_same` だけ 3 数すべてが小さく、帯で切り出せる |
| 8 | 0.65 | 0.90 | 機械化可能 | 立式ペアは共通 4 肢から 1 つに固定 (2 問で 1 問)、normal 2 問は回答欄が曜日か数値かで切り分け、診断は 4 択から 1 つ | 設計側 T1 0.45 / T2 0.55。normal 2 問の与件の形は揃ったが答えの型が違う (H5) |
| 9 | 0.40 | 0.95 | 機械化可能 | T1 は normal を常に最大公約数、find_all を 1 種に固定、診断は 4 択から 1 つ。T2 は最大公約数と最小公倍数の語で normal と find_all を取り、診断は「わけ」欄と単位から向きを決めて W 表を引く | 設計側 T2 0.45。表層形衝突は場面の向きが分かれば解け、向きは単位と与件の数から無償で出る |
| 10 | 0.55 | 0.95 | 機械化可能 | Lv4 と同じ手続きに、整列は設計を記憶した定数列、find_all は候補の帯で公約数側か公倍数側かを決めるを足す | 設計側 T1 0.45 / T2 0.50。H2 と H3 |

閾値超えは 10 段中 6 段。0.90 以上 (機械化可能) が Lv4、5、6、8、9、10 の 6 段、0.74 以下 (耐性あり) が Lv1、2、3、7 の 4 段。0.75 から 0.89 の段は無い。

耐性ありと判定した 4 段には条件が付く。いずれも 5.4 章 H1 (ペア内の出題順が無規定) が成立していないことを前提にした値であり、順序が固定なら Lv1 は 1.00、Lv2 は 1.00、Lv3 は 0.80 に戻る。

#### 5.1.1 値の内訳

Lv1 (T1 0.60)。normal 3 問のうち `div_count` は列を持たない唯一の問題で、4.2 章が「normal 3 問のうち 2 問が数と列の両方を持つこと」と書いているためこの分離は規則で保証されている。1 問取れる。`div_missing` と `div_extra` は本文も列も同一なので固定手続きは 1 問しか取れない。find_all の `div_select` と `nondiv_select` は補集合なので同じく 1 問。合計 3 問。

Lv2 (T1 0.60)。find_all のペアで 1 問。normal 3 問のうち `mul_nth` は 9.2 章の帯 (N は 3 から 15、k は 5 から 15、積は 200 以下) により与件の最大値が 15 以下で、`mul_count` と `mul_capped` (上限 M は 40 から 120) と重ならない。帯を機械照合して 1 問取り、残る P7 ペアで 1 問。合計 3 問。4.2 章の新要件は与件の個数を 2 に揃えたが、監査 1.1 章が T1 に許した手がかりは個数と大小の両方である。

Lv3 (T1 0.40)。normal 3 問は与件がすべて 2 数で帯も共通 (common の a, b は 6 から 60) のため分離できない。P2 ペアで 1 問。`common_div_count` は取れない。find_all は候補に公約数と公倍数が混在しており帯の分離が消えているのでペアで 1 問。合計 2 問。v0.2 でここが最も効いている。

Lv4 (T1 0.67、T2 1.00)。T1 は例 3 (与件 3 数、第 3 数が小、単位が秒) と例 5 (与件 3 数、第 3 数が 2 数より大) を確定的に取り、例 4 は立式で正解が 4 肢中 3 つある散文肢のどれかなので 0.33、例 1 と例 2 のペアで 1 問。合計 3.33 問。T2 は 5 問すべて。手続きは 5.4 章 H2 に書く。

Lv5 (T1 0.64、T2 0.90)。整列 1 問目は D1 から D5 のいずれでも数値順で並ぶので 0.9。整列 2 問目は数ゼロ設計で、T1 (記憶を持たない) では 4 部品の 24 通りから 0.04、T2 (定数列を 1 度覚える) では 1.00。normal 2 問は列の種類が違う (候補の列と素因数分解の等式) ため、列の積が先頭の数に等しいかを見れば分離でき 2 問。診断は 0.25。

Lv6 (T1 0.52、T2 0.95)。T1 は例 3 (3 数、秒) で 1 問、例 5 (立式、第 3 数が大) で散文肢 2 つから 0.5、例 2 (normal、2 数) で 0.5、診断 2 問で 0.25 と 0.33。T2 は 5 問すべてを取るが、プールの揺れを見て 0.95 とした。

Lv7 (T1 0.66)。normal 3 問は与件が 3 数に揃ったが、`rem_same` は 3 数すべてが 12 以下 (9.2 章の p, q は 2 から 12、r は min 未満)、`rem_zero` と `rem_count` は 50 以上の数を 1 つ持つ。帯で `rem_same` を切り出して 1 問、残り 2 問で 1 問。find_all は 3 種の与件の帯が違う (`rem_divisor` は 20 から 120 の大きい数を持ち、`rem_zero_select` は小さい 2 数、`rem_same_select` は小さい 3 数) ので 0.8。立式は 4 肢のうち引き算が 2 つなので 0.5。合計 3.3 問。

Lv8 (T1 0.65)。立式ペアは選択肢集合が完全に一致したので 2 問で 1 問。normal 2 問は与件の形 (日数 1 つと曜日 1 つ) が揃ったが、`cycle_weekday` の答えは曜日名、`cycle_count` の答えは数値で、回答 UI が違うため 2 問とも取れる。診断は 0.25。

Lv9 (T1 0.40、T2 0.95)。normal 2 問は与件が 2 数に揃い帯も共通なので 1 問。find_all は 2 種からの抽選で、候補に公約数側と公倍数側が混ざっているので 0.5。診断 2 問は T1 では 0.25 ずつ。T2 では 5 問すべてに近い。

Lv10 (T1 0.55、T2 0.95)。整列は T1 で 0.04、T2 で 1.00。例 3 (3 数、第 3 数が大) で 1 問。find_all は候補がすべて 18 以下で公約数側の帯にあるので 0.9。立式は散文肢 2 つから 0.5。診断は 0.3。

### 5.2 運用注の Lv1 延長についての独立判定

判定: 適用する。ただし設計側が挙げた根拠は採らない。

まず設計側の主張の事実確認から入る。11 章は Lv1 が `div_count` / `div_missing` / `div_extra` / `div_select` / `nondiv_select` の 5 手続きに割られたと書く。これは正確でない。5 つはいずれも「N の約数の集合 D(N) を作る」という 1 つの計算を実行し、その出力の読み方が違うだけである。個数を答える、D(N) から列を引く、列から D(N) を引く、候補と D(N) の共通部分を取る、候補から D(N) を引く。数学的な内容は同一で、異なるのは出力の射影だけである。したがって「5 手続き」という記述は「1 手続きと 5 通りの読み出し」に訂正されるべきである。

この訂正は T1 の値を変えない。T1 が Lv1 で 0.60 に留まるのは、5 つが別の計算だからではなく、`div_missing` と `div_extra` の本文と列が完全に同一であること、および `div_select` と `nondiv_select` の候補が同一で正解集合が補集合であることによる。読み出しが構造的に見分けられない、という一点で値が決まっている。結論は変わらないが、設計側が示した理由は成立していない。

次に運用注そのものの適用可否を見る。判断基準は、その語彙の分岐が、その Lv の学習対象そのものを代替するのか、それとも学習対象を実行したうえで出力のどれを返すかを選ぶだけなのかに置いた。

Lv4 の大小語は前者である。Lv4 の学習対象は場面から向きを決めることであり、「いちばん大きい」から最大公約数へ飛ぶ表は、その判断を丸ごと肩代わりする。表を覚えた子は判断を一度もしないまま正答する。だから運用注を適用しない。

Lv1 の「ぬけている」「まざっている」「でない」は後者である。どの分岐に入っても D(N) を作らなければ答えは出ない。語彙表だけを持ち約数を知らない子は、5 問のいずれも解けない。語彙は学習対象を代替せず、出力を選んでいるだけである。この構造は Lv2 の「倍数」と「約数」、Lv3 の「最大公約数」と「最小公倍数」と同じで、前回 1.2 章で運用注を適用した理由と一致する。

ただし延長の根拠を「監査が Lv2 と Lv3 に適用したから」に置くのは受け入れない。運用注の本文が保護しているのは「量名や単位の定義、問い文が名指しする量の読み取り」である。「約数」と「倍数」は量名であり本文に直接あたる。「ぬけている」「まざっている」は量名ではなく、提示された列に対する集合演算の名である。文言の上では運用注の保護範囲の外にあり、先例の機械的な延長では通らない。適用してよいのは、上に書いた「学習対象を代替しない」という別の理由による。設計側は自分の案件について適用可否を判断したうえ、その理由も運用注の文言に照らすと成立していない。同じ形の延長を `next` から最小公倍数への写像に適用すれば誤りになるが、その誤りは v0.2 自身が 3.4 章で認めている。線引きは先例ではなく代替性で引く。

両方の数値を示す。

| Lv | 運用注を適用した場合 | 適用しない場合 |
|---|---|---|
| 1 | T2 は不問。判定は T1 0.60 で耐性あり | T2 1.00 で機械化可能 |
| 2 | T2 は不問。判定は T1 0.60 で耐性あり | T2 1.00 で機械化可能 |
| 3 | T2 は不問。判定は T1 0.40 で耐性あり | T2 1.00 で機械化可能 |
| 7 | T2 は不問。判定は T1 0.66 で耐性あり | T2 1.00 で機械化可能 |

Lv7 も併記した。「わり切れる」と「あまる」はわり算の定義の読み取りにあたるので適用する。ただし Lv7 の学習対象である「割る数を求めるなら余りを引く」は語彙の外にあり、そこは立式の選択肢の形から取られている (T1 0.66 の内訳)。適用の可否が判定を動かす段ではない。

なお Lv1 の T1 0.60 は 5.4 章 H1 が成立していないことを前提にしている。ペア内の出題順が固定なら Lv1 の T1 は 1.00 になり、運用注の適用可否は結論に関係しなくなる。運用注より先に閉じるべきはそちらである。

### 5.3 前回指摘の解消状況

| # | v0.1 の指摘 | 状態 | 根拠 |
|---|---|---|---|
| S1 | 反転 A が 3 数を持つ唯一の型 | 閉じていない | 3 数を持つ pattern は 3.1 章で 13 種に増えた。しかし反転は上限または下限を数学的に要求するため反転問題は必ず 3 数、一致問題は必ず 2 数になる。写像は「3 数なら反転」で復元される (5.4 章 H2)。さらに第 3 の数の帯が分離しており、上限 C は 9.2 章の範囲で必ず max(a, b) より大きく、下限 L と順位 k は必ず min(a, b) より小さい。全数検査で例外ゼロを確認した |
| S2 | 反転 1 問以上では検証 35 を満たせない | 規則は閉じた。テストは閉じていない | 4.2 章が反転 2 問以上でうち 1 問以上を反転 B と定め、規則と検証の両立は回復した。ただし検証 35 の敵ソルバー定義が「3 数なら上限つき公倍数」のままで、実際に成立する攻撃 (3 数なら反転) より弱い。定義された攻撃は落ち、成立する攻撃は検出されない |
| S3 | `div_of` の答えが列の最大値 | 閉じた | `div_of` を廃止し `div_missing` と `div_extra` に置換。答えは列の最大値ではなくなった |
| S4 | 対比ペアが選択肢集合と答えの型で割れる | 部分的に閉じた | 4.3 章に選択肢集合と答えの型の一致を追加し、Lv8 の 4 肢を共通化した。ただし同じ 4.3 章の「片方が診断の場合」の項と両立せず、Lv6 と Lv9 の例示セットが自分の規則に違反している (H7)。またペア内の出題順が無規定で、こちらのほうが強い漏洩になる (H1) |
| S5 | 診断が答案の表層形照合で取れる | 部分的に閉じた | 8.2 章に衝突の正本ができ、9 組が登録された。式だけを見るソルバー (T1) は 0.5 に落ちる。ただし 9 組すべてが場面の向きまたは問い文の 1 語で解け、向きは単位と与件の数から無償で出る。T2 は落ちない。8.2 章の Lv8 の 1 組 (50 ÷ 7 の商、回数を問うか曜日を問うか) は答えの型が違うため T1 でも解ける |
| S6 | Lv9 で `correct_alternative` の実在が答えを教える | 閉じた | 4.2 章の最低要件から外し、4.8 章で誤答肢としても使うことにした。検証 22 が正解率 40% 以下を要求する。Lv9 例 4 に誤答肢としての実例がある。残余は、肢がある問題で常にその肢を選ぶ手続きが 0.40 まで得られる点だが閾値を超えない |
| S7 | 足場が向きを名指しする | 閉じた | 4.1 章に向き中立の規則を追加し、本問の与件の数を使わないことも規定。検証 28 が両方を見る。Lv7 と Lv9 の例示も直っている |
| S8 | 形式が型を漏らす | 部分的に閉じた | 検証 17 が 1 問しかない形式枠に 2 種以上を要求する。ただし本文が名指しするのは Lv7 の find_all、Lv9 の find_all、Lv10 の ordering の 3 枠だけで、Lv5 の diagnosis、Lv6 の formulation、Lv8 の diagnosis、Lv10 の normal と find_all と formulation と diagnosis は 1 問枠のまま名指しされていない。検証項目の文面が「1 問しかない形式枠について」と一般形で始まるので実装で救える可能性はあるが、例示された 3 枠しか 4.2 章に対応する要件が無い |
| S9 | doc 内の自己矛盾 5 件 | 4 件閉じ、1 件残存 | T4 の 3 問 (閉じた。3.3 章を 1 問までに締め例示も直した)、数なし部品の位置の乱数と位相順序の矛盾 (閉じた。位置は設計の意味で決めることにした)、Lv7 の 4 肢の演算子 (閉じた。35 − 1 を追加)、remainder の互いに素 (閉じた。pattern 別に書き分け)。残存は find_all の誤答候補に「もう一方の向きの集合に属する数」を含める規定で、Lv1 の例 4 と例 5 (候補に 24 の倍数が無い) と Lv10 の例 1 (候補に 12 と 18 の公倍数が無い) が満たしていない。9.2 章と検証 27 は残っている |
| S10 | 整列が 2 設計で部品数から判別できる | 部分的に閉じた | 設計は 7 種になり (部品数, 数なし部品の位置) の組が重なった。ただし攻撃は設計の同定を必要としない。D3 と D4 は組が同じだが並べ方の手続きも同じなので、区別できないことが防御にならない。D1 から D5 はいずれも数値順 (連鎖、素数の昇順、問題文の数の出現順) で並ぶ。D6 と D7 は数値照合が効かない代わりにパラメタを持たず正順が定数になった (H3) |
| C1 | 整列の規則がソルバーになる | 部分的に閉じた | S10 と同じ。数ゼロ設計は数値照合を無効化したが、無効化と引き換えに問題そのものを定数にした |
| C2 | 正解位置の無規定 | 閉じた | 4.6 章が選択肢、find_all の候補、整列の初期提示順の 3 か所を規定し、検証 25、26、27 が見る。前回 4 章で限界として挙げた 2 件がここで閉じた。残るのはセット内の出題順とペア内の順序で、これは C2 の系ではあるが v0.2 で新たに露出した (H1) |
| C3 | 固定語彙が lookup table になる | 部分的に閉じた | `next` を廃し `nth` に両向きの実体を与えたのは有効で、問い語 4 型はすべて両向きを持つ。ただし 3.3 章が「単位から向きが決まる状態は T4 に閉じている」と明記しており、分と秒は依然として公倍数側を名指しする。8 章の W 表から答案表層形への写像は 8.2 章で関数でなくなったが、向きを与えれば再び関数になる |
| C4 | セット内の型分散が無い | 閉じた。ただし逆向きの露出が増えた | 全 10 段で配合が「5 種を 1 問ずつ」などの形に固定され、空間サイズは 8 段で 5 以上、上限は 8 段で 1 になった。分散としては v0.1 より強い。一方で配合が完全に決まったことにより、セット内の位置から pattern を引ける余地が増えた (H1 の後半) |
| 横断 1 | 反転要件を 2 問以上に | 実施済み。効果は限定的 | S1 と S2 のとおり |
| 横断 2 | 検証 32 の敵ソルバー定義の強化 | 実施済み。定義が弱い | 検証 37 は「部品に数が無い場合は等確率で置換を選ぶ」と書いており、D6 と D7 を攻撃対象から外している。実際には定数列の記憶で 1.00 が取れる |
| 横断 3 | 検証 30 の敵ソルバー定義の強化 | 実施済み。定義が弱い | 検証 35 は前処理として「3 数なら上限つき公倍数」を持つが、成立する攻撃は「3 数なら大小語を反転」である。前者は gcd 側の 3 数問題で必ず外れ、後者は外れない |

### 5.4 v0.2 で新設された規則が作った新しい穴

H1 から H5 が v0.2 の新設要素に由来する。H6 から H9 は v0.1 から続く穴が形を変えて残ったものである。

H1 (対比ペアの出題順が無規定)。4.3 章はペアを全 10 段に広げ、Lv2 と Lv3 では 2 組を義務づけた。ペアの防御は「同じ数で問いだけが違う 2 問を並べ、固定手続きに片方を落とさせる」ことにあるが、これはペア内のどちらが先かが分からない場合にだけ成立する。順序が固定なら「隣接する 2 問のうち先のほうには手続き X、後のほうには手続き Y」で 2 問とも取れる。doc はここを規定していない。4.6 章は選択肢と候補列と整列部品の 3 か所のシャッフルを規定し、しかも「doc が要求していなければ実装で落ちうる」と自ら書いているのに、ペアの順序と出題順は要求していない。6 章の例示は Lv6 と Lv8 で出題順を明記しており、向きの対比 3 型 (P2、P3、P6) ではすべて公約数側が先に置かれている。効きは 1 組あたり 0.20 で、Lv1 が 1.00、Lv2 が 1.00、Lv3 が 0.80、Lv4 が 0.87、Lv8 が 0.85 になる。配合が「5 種を 1 問ずつ」に固定された弾層 3 段では、さらに強い形 (セット内の位置から pattern への全単射) が成立しうる。

H2 (反転と与件 3 数の同値)。4.2 章は「3 数の問題が gcd 側と lcm 側に各 1 問以上」を要求し、4.4 章は「数の個数から向きへの写像も関数にならない」と書く。数の個数から向きへの写像は確かに関数でなくなったが、写像は消えたのではなく形を変えた。一致問題 (max が gcd 側、min が lcm 側) は上限も下限も要らないので必ず 2 数である。反転問題は数学的に境界を要する。上限の無い「いちばん大きい公倍数」は存在せず、下限の無い「いちばん小さい公約数」は常に 1 だからである。したがって反転問題は必ず 3 数になる。ソルバーは「与件が 2 数なら大小語を素直に読み、3 数なら反転する」の 1 行を足せばよい。6 章の場面 3 段の例示セットに対してこの手続きは全問正答する。これは pattern を足しても閉じない。閉じるには一致問題にも意味の無い第 3 の数を持たせるか、境界を数以外の形で与える必要がある。

H3 (数ゼロ設計が定数になった)。4.7 章と 9.5 章は D6 と D7 を新設し、部品に数を 1 つも置かないことで数値照合を原理的に無効化した。無効化は成立している。しかし D6 と D7 は数を持たないと同時にパラメタも持たない。`procedure_gcd` の 4 部品は毎回同じ 4 つの文であり、正順は定数である。監査 1.1 章が T2 を定義するときの言い方 (1 回覚えれば理解なしに全問通る有限表) にそのまま当てはまる。参照点にした kom_johou_seiri の Lv7 は、部品が本文から作られるため毎回変わり、そこが 0.22 の理由だった。数を抜いただけでは同じ強度にならない。4.2 章は Lv5 で数ゼロ設計を 1 問以上と義務づけ、Lv10 でも抽選対象に入れているので、この定数は毎セット 1 問の規模で効く。

H4 (「わけ」欄が表層マーカーになった)。4.8 章は `word_cue` を正解ラベルとして成立させるために答案を 3 欄にした。8 章は `word_cue` と `swap_gcd_lcm` の違いを「答案に添えた理由」に置き、両方が同時に成立する答案は作らないと書く。この 2 つを合わせると、「わけ」欄に大小語の引用があることが `word_cue` の必要十分条件になる。6 章の実例 (Lv6 例 4 と Lv10 例 5) はいずれも「「いちばん少ない」と書いてあるから」で始まる。同じ欄が `correct_alternative` にもマーカーを与えており、Lv9 例 2 の「2 数の積を最大公約数でわると最小公倍数になる」のように一般則を述べる「わけ」は正しい道の側にしか現れない。8.2 章が式の表層形の一意性を壊した一方で、新しい欄が別の一意マーカーを作っている。

H5 (Lv8 の normal 2 問が答えの型で割れる)。改版記録 17 は、normal 2 問の与件の形を揃えるために `scene_lcm_time` を `cycle_weekday` に置き換えたと書く。置き換えの結果、normal 2 問は `cycle_weekday` (答えは曜日名) と `cycle_count` (答えは数値) になった。4.3 章は答えの型の一致を対比ペアに義務づけたが、この 2 問はペアではない (ペアは立式 2 問) ため規則が掛からない。回答欄が曜日の選択か数値の入力かは出題画面から見えるので、2 問とも読まずに手続きを選べる。S4 で塞いだ経路が、S4 の修正の副作用として同じ Lv の別の 2 問に移動した。

H6 (揃えたのは個数だけで帯は分離したまま)。4.2 章は Lv2、3、5、7、8、9 に「与件の形を揃える」要件を新設した。揃えたのは与件の個数と、Lv5 では列の有無である。監査 1.1 章が T1 に許した手がかりは「出現した数の個数・大小・比」であり、個数だけを揃えても大小が残る。実測すると、Lv2 は `mul_nth` の k (5 から 15) と `mul_count` の上限 (40 から 120) が重ならず、Lv7 は `rem_same` の 3 数がすべて 12 以下で他の 2 種は 50 以上を含み、Lv4 と Lv6 と Lv10 は上限 C が必ず max(a, b) より大きく下限 L と順位 k が必ず min(a, b) より小さい。最後の点は 9.2 章の帯を全数検査して例外ゼロを確認した。Lv5 は列の有無を揃えたが、一方は候補の並び、他方は素因数分解の等式で、列の積が先頭の数に等しいかを見れば分かれる。

H7 (4.3 章の新条件が同章の別条件と両立しない)。4.3 章は「2 問の答えの型が一致すること」を新設し、同時に「片方が診断の場合は、答案の答えではなく、もとの問題の答えを比較する」を残した。診断の答えは選択肢の添字、normal の答えは数値なので、診断と normal の組は型が一致しない。6 章の Lv6 (例 1 が診断、例 2 が normal) と Lv9 (例 1 が normal、例 2 が診断) の対比ペアは、どちらも新しい条件に違反している。検証 6 は両方を検査するので、実在証明 (付録 A) はこの 2 段で落ちる。どちらの条件を残すかの決定が要る。

H8 (敵ソルバーの定義が攻撃より弱い)。検証 35 と 37 は v0.1 の監査が手で行った攻撃を回帰テストに固定したものだが、固定された定義は実際に成立する攻撃より弱い。検証 35 の前処理は「3 数なら上限つき公倍数」で、H2 の「3 数なら反転」ではない。検証 37 は「部品に数が無い場合は等確率で置換を選ぶ」と書き、H3 を攻撃対象から外している。検証 38 は答案の式だけを見るソルバーを 0.50 未満に抑えるよう求めるが、向きを単位と与件の数から取るソルバーは対象外である。3 本とも、v0.2 の規則が通ることを確認する形に書かれている。敵ソルバーは修正のたびに攻撃側を更新しないと、規則の自己証明になる。

H9 (単位から向きへのオラクルが明文で残った)。3.3 章は T4 を 1 問までに締めたうえで「T4 以外のテンプレートで時間の単位を使ってよい。単位から向きが決まる状態は T4 に閉じている」と書き、T1、T2、T3、T5 では分と秒を使わないと定めた。これは分または秒が現れたらその問題は公倍数側だと確定させる規定である。単位記号の見た目は監査 1.1 章が T1 に許した手がかりなので、T2 ですらない経路が 1 セットに 1 問残る。さらに 4.2 章の新要件「大小語を持たない問題 1 問以上」を満たす実体として、6 章は Lv4 と Lv6 の両方で `scene_lcm_nth` (ライトが同時に光る、単位は秒) を選んでいる。大小語ソルバーから 1 問を隠すために置いた問題が、単位ソルバーには無償で渡っている。

### 5.5 残る修正案

閾値を下回らせる最小の手を左に、目的まで満たす案を右に置く。

| 対象 | 最小修正 | 目的まで満たす案 |
|---|---|---|
| H1 (全 10 段) | 4.3 章に「ペアの 2 問の出題順を注入 `random` で決める」を追加し、検証 6 に順序の一様性を足す | セット内の 5 問の出題順そのものを、4.1 章 (足場は前半 2 問)、4.5 章 (連鎖は認識が先)、隣接相異の制約下で注入 `random` により決めると規定する。配合が完全に固定された以上、順序が唯一の自由度である |
| H2 (Lv4、6、10) | 一致問題にも第 3 の数を持たせる。`scene_gcd_size` の `ask: max` に「1 辺は 1cm より長い」のような答えに影響しない下限を、`scene_lcm_size` の `ask: min` に上限を付け、与件の数を全問 3 つに揃える | 境界を数で与えない。上限と下限を「板におさまる」「2 人では分けられない」のように場面の条件として書く。単位換算を持ち込まない書き方に限る必要がある (2.3 章の規約 4)。あわせて検証 35 の敵ソルバーを「3 数なら大小語を反転」に置き換える |
| H3 (Lv5、10) | D6 と D7 の部品文にパラメタを入れる。手順の対象を「2 つの数」から具体の 2 数にし、部品の並びが数に依存しない形のまま文言だけを変える | 数ゼロ設計を johou_seiri Lv7 型にする。すなわち部品の集合そのものを問題ごとに変える (手順を 4 通りの言い回しから抽選し、無関係な手順を 1 つ混ぜて選別も要求する)。あわせて検証 37 の「部品に数が無い場合」の除外を外し、記憶ソルバーを敵に加える |
| H4 (Lv6、9、10) | `word_cue` 以外のラベルが正解になる答案にも「わけ」欄に大小語の引用を置く。すなわち引用の有無をラベルと独立にする | 8.2 章の衝突表に「わけ」欄の衝突を足す。同じ「「いちばん少ない」と書いてあるから」という理由が、反転問題では `word_cue`、一致問題では `correct` になる組を作る |
| H5 (Lv8) | 4.2 章の Lv8 の要件を「normal 2 問は与件の形と答えの型を一致させる」に直す。`cycle_weekday` を normal に置くなら曜日名で答える問題を 2 問にする | 4.3 章の答えの型の一致を、ペアだけでなく同一形式の複数問へ広げる |
| H6 (Lv2、4、5、6、7、10) | 「与件の形を揃える」の定義に帯を含める。9.2 章の範囲を pattern 間で重ねる (`mul_nth` の k を 40 以上まで許す、`rem_same` に 50 以上の数を持たせる、上限 C と下限 L の帯を重ねる) | 検証項目に「同一形式の n 問について、与件の数の個数と、各位置の数の帯が重なること」を新設する。個数だけを見る現在の検証 5 では検出できない |
| H7 (Lv6、9) | 4.3 章の答えの型の一致から診断を含む組を除外し、そのうえで診断と normal の組が形式の違いで割れないことを別途担保する | 診断を対比ペアの相方に使うのをやめ、対比ペアは同一形式どうしに限る。Lv6 と Lv9 の形式配合を組み直す必要がある |
| H8 (全段) | 検証 35、37、38 の敵ソルバー定義を本章の手続きに差し替える | 敵ソルバーの定義を doc から外し、監査側が保持する攻撃集合として管理する。doc に書いた攻撃は次の改版で必ず通るように設計されるため、回帰テストとしての寿命が 1 版しかない |
| H9 (Lv4、6、10) | T4 の場面から分と秒を外し、「何回目に重なるか」を長さや個数で書く。あるいは T4 以外にも分と秒を許し、公約数側の場面 (同じ時間ずつに区切る) を作る | 3.3 章の「単位から向きが決まる状態は T4 に閉じている」という設計判断を撤回する。T4 を残すなら、公約数側の時間場面 (60 分の作業を等分する) を対にして足す |
| S8 の残り | 検証 17 の対象を「1 問しかない形式枠のすべて」と明記し、Lv5 の diagnosis、Lv6 の formulation、Lv8 の diagnosis、Lv10 の 4 枠にも 4.2 章の抽選要件を書く | 同左 |
| S9 の残り | 9.2 章の「誤答候補はもう一方の向きの集合に属する数を 1 個以上含める」を Lv1 と Lv10 の例示に反映するか、divisor 領域と scene 領域については規定を書き換える | 同左 |

### 5.6 再監査の限界

1. H1 の効きは、生成器がペア内の順序と出題順をどう決めるかに完全に依存する。doc が規定していないため、本監査は順序が固定である場合と乱数である場合の両方の値を出し、判定表には乱数である場合 (設計側に有利な側) を採った。
2. 診断の T2 値は、答案の「わけ」欄と単位と与件の数から場面の向きが取れるという前提に立つ。この前提を外して診断を一様推測 (0.25) とすると、Lv6 の T2 は 0.70、Lv9 は 0.70 に下がり、両段は耐性ありになる。Lv5、8、10 は診断以外で 0.75 を超えるため判定は動かない。
3. 数ゼロ設計を「1 度覚えれば取れる定数」として T2 に数えるかどうかは、監査 1.1 章の T2 の定義 (doc が固定表として明記した型語彙にのみ分岐する) の解釈による。部品文は 9.5 章に明記された固定文なので表として扱ったが、これを表と見ない立場なら Lv5 の T2 は 0.70、Lv10 は 0.75 になる。
4. 立式の選択肢を T1 がどこまで扱えるかは前回と同じ未決点である。本監査は「算用数字の式に見える肢を除外して散文の肢から選ぶ」までを T1、「最大公約数と最小公倍数を見分ける」を T2 とした。この線を動かすと Lv4、6、10 の T1 が 0.1 前後動く。
5. 検証 5 と 6 が実際に何を検査するかは実装に依存する。とくに H7 (4.3 章の内部矛盾) は、実装が新条件を無視して v0.1 の定義で通す可能性がある。doc からは決まらない。
6. 付録 A (実在証明) が未作成のため、6 章の例示セットが 4.2 章と 4.3 章の全要件を同時に満たすかは doc の中で保証されていない。本監査が見つけた違反は H7 と S9 の残りの 2 件だが、網羅的に検査したわけではない。

## 6. 三次監査 (v0.3 対象)

- 対象: `docs/komorebi_seisu_curriculum.md` v0.3 (1590 行、2026-08-28)
- 範囲: 依頼により 4 点に絞る。前回指摘 H1、H4、H5、H6、H7、H9 の解消確認、H2 の残存量の計算、Lv1 から Lv3 と Lv7 の T2 への運用注の適用可否、v0.3 の新設要素が作った穴。H3 (数ゼロ設計の正順が定数) は `komorebi/app.js` の制約により選別形が実装できないことを依頼側が実コードで確認済みのため、監査対象から外し、整列が johou_seiri Lv7 並みに落ちない前提で他段の埋め合わせだけを見た
- 設計側の自己申告: T1 は 0.40 から 0.60、T2 は Lv4 以降が 0.55 から 0.70、閾値超え 0 段。Lv1 から Lv3 と Lv7 の T2 は 1.00 のままで運用注の適用を監査に委ねている

### 6.0 結論 3 行

1. 閾値超えは 0 段である。設計側の主張を支持する。運用注を適用したうえでの最大値は Lv5 の 0.71、次いで Lv9 の 0.64、Lv7 の 0.62 で、いずれも 0.75 を下回る。ただし Lv5 は昇降の均衡点 0.70 を超えており、余裕は 0.04 しかない。T1 は全 10 段で 0.62 以下に収まり、設計側の見積りとの差も最大 0.12 に縮んだ。前 2 巡で差の中央値が 0.20 だったことと比べると、自己見積りの精度自体が上がっている。
2. H2 の残存は、設計側が心配した経路では 0 である。実体が片側しか無い 2 象限はどちらも一致問題の象限であり ((max, 上限が小) は公約数側で max と gcd の一致、(min, 下限が大) は公倍数側で min と lcm の一致)、大小語ソルバーはその問題をもともと取っている。1 問以下の制限は必要十分どころか、この攻撃に対しては掛けなくても値が変わらない。実際の残存は別のところにあり、Lv4 の pattern 集合に公約数側の大小語なし pattern が無いため、4.4.2 章の規則 2 が要求する 1 問が常に公倍数側になる。Lv4 の大小語ソルバーの上限は doc が書く 0.40 ではなく 0.60 である。
3. 新しい穴は 8 件で、うち v0.3 の新設要素に由来するものが 5 件である。設計を変えなければ閉じないものは無い。3 件は付録 A を書く前に直す必要があり (例示セットのパラメタ帯違反、Lv5 の与件の形、Lv8 の固定出題順)、いずれも例示と 1 行規則の修正で足りる。

### 6.1 v0.3 判定表

T1 は構造盲目ソルバー、T2 は全語彙ソルバー、T2 (運用注) は保護語彙による正答を除いた値である。保護と非保護の線引きは 6.2 の 3 で定義した。判定は T2 (運用注) で行う。

| Lv | T1 | T2 | T2 (運用注) | 判定 | ソルバーの手続き (1 行) | 設計側の値との差 |
|---|---:|---:|---:|---|---|---|
| 1 | 0.60 | 1.00 | 0.60 | 耐性あり | 列を持たない normal は約数を数え、列を持つ 2 問とfind_all 2 問はそれぞれ片方の読み方に固定する | 差なし |
| 2 | 0.56 | 1.00 | 0.56 | 耐性あり | 与件の最大値が 61 以上なら上限系、29 以下なら k 番目、30 から 60 なら固定手続き。find_all のペアで 1 問 | 設計側 T1 0.45。帯の重なりが 30% 以上という下限規定のため、残り 70% では帯が分離したまま (6.4 の N8) |
| 3 | 0.40 | 1.00 | 0.40 | 耐性あり | normal は 3 種のうち 1 つに固定、find_all は公約数側に固定 | 差なし |
| 4 | 0.47 | 0.60 | 0.60 | 耐性あり | 大小語が無ければ公倍数側、あれば素直な写像。立式は散文肢から選ぶ | 設計側 T2 0.55。大小語なしの枠が Lv4 では常に `scene_lcm_nth` になる (6.4 の N1) |
| 5 | 0.58 | 0.90 | 0.71 | 耐性あり (最薄) | 整列の数あり設計は数値順で並べ、数ゼロ設計は手順の役割語で並べる。normal 2 問は式の後ろの数の個数で分け、診断は答案の式を W 表で引く | 設計側 T1 0.50 / T2 0.70。`factor_count_cond` の条件節が数を 1 つ余分に持つ (6.4 の N4) |
| 6 | 0.40 | 0.54 | 0.54 | 耐性あり | Lv4 と同じ。診断は「わけ」欄の衝突により向きを決めないと引けない | 設計側 T2 0.60 とほぼ一致 |
| 7 | 0.62 | 1.00 | 0.62 | 耐性あり | normal 3 問は帯の重なりの外側で 2 問を切り分け、find_all は与件の帯で 3 種を分け、立式は引き算の 2 肢から選ぶ | 設計側 T1 0.50。帯の重なりが 30% 止まりであることは Lv2 と同型 |
| 8 | 0.45 | 1.00 | 0.58 | 耐性あり | 立式ペアは共通 4 肢から 1 問、normal 2 問は答えの型が揃ったので 1 問、診断は答案の式を W 表で引く | 設計側 T2 0.70。全語彙では 1.00 だが、問い文が名指しする量の読み取りが大半を占める |
| 9 | 0.40 | 1.00 | 0.64 | 耐性あり | normal 2 問で 1 問、find_all は 2 種から 0.5、診断は場面問題が常に公倍数側であることと答案の式から引く | 設計側 T2 0.65 とほぼ一致。ただし内訳が違う (6.4 の N6) |
| 10 | 0.45 | 0.55 | 0.55 | 耐性あり | Lv4 と同じ。整列は設計により 0.2 から 0.9 | 設計側とほぼ一致 |

閾値 0.75 を超える段は無い。0.70 を超える段は Lv5 の 1 段である。

### 6.2 依頼された 4 点の判定

#### 1. H1、H4、H5、H6、H7、H9 の解消

| # | 状態 | 根拠 |
|---|---|---|
| H1 (ペア内と出題順が無規定) | 閉じた | 4.3 章がペア 2 問の出題順を注入 `random` と定め、4.6 章がセット内 5 問の出題順も乱数化した。制約 3 つ (足場は先頭 2 問、連鎖は認識が先、隣接相異) を満たす順列から一様に選ぶと明記し、検証 15 が位置分布を見る。足場を順序決定の後に付けるという書き方も、どの pattern が足場を持つかを回ごとに変える効果がある |
| H4 (「わけ」欄が一意マーカー) | 閉じた | 8.2 章に「わけ」欄の衝突表を新設し、大小語を引用する理由を一致問題にも付けた。同じ理由文が一致問題では `correct`、反転問題では `word_cue` になる。ラベルを決めるのが理由文ではなく理由が場面に対して正しいかどうかになったので、欄を見るだけでは引けない。しかも解くには向きの判断が要り、それは大小語ソルバーが確定的に外す側である |
| H5 (Lv8 の normal が答えの型で割れる) | 閉じた | `cycle_weekday_back` を新設し、normal 2 問を「何日後」と「何日前」にして答えの型を曜日名で揃えた。4.2 章の Lv8 の要件にも「与件の形と答えの型を一致させる」と書いた |
| H6 (揃えたのが個数だけ) | 部分的に閉じた | 9.2 章に帯の重なりを書き、検証 7 を新設したのは有効である。ただし要求が「重なる区間から 30% 以上を引く」であり、残り 70% では帯が分離したままになる。Lv2 の `mul_nth` (k は 4 から 60) と `mul_count` の上限 (30 から 300) は重なる区間が 30 から 60 だけで、それ以外では最大値を見れば切り分けられる。Lv7 も同型。Lv5 は `prime_check` を `factor_count_cond` に替えて形を揃えたが、条件節が数を 1 つ余分に持つ (6.4 の N4) |
| H7 (4.3 章の内部矛盾) | 閉じた | ペアを同一形式に限り、Lv6 と Lv9 のペアを normal どうしに組み直した。Lv10 は 5 形式 1 問ずつではペアが組めなくなるため、整列 1 問に残り 4 問を 4 形式から取り 1 形式だけ 2 問にする形へ変え、5.2 章の表もそれに合わせた。副次的に Lv10 の配合が固定でなくなった |
| H9 (単位から向きへのオラクル) | 閉じた | 3.3 章の T4 に公約数側の実体 (決まった長さの時間を同じ長さのくぎりに分ける) を与え、1 セット 1 問の制限も分秒の使用制限も外した。検証 10 が「単位から `direction` への写像が全生成にわたり関数にならない」を見る |

#### 2. H2 の残存量

四象限のうち 2 つが数学的に空であることは事実であり、消せない。しかしそこから引ける 2 本の規則は、大小語ソルバーに何も足さない。

(max, 上限が小) の象限は公約数側にしか実体を持たない。この象限の問題は `ask` が `max` で `direction` が gcd なので、定義により一致問題である。(min, 下限が大) の象限は公倍数側にしか実体を持たず、`ask` が `min` で `direction` が lcm なので、これも一致問題である。大小語ソルバー (max なら公約数、min なら公倍数) はどちらの問題も素直な写像で取っている。したがって「上限が小なら公約数側」「下限が大なら公倍数側」という 2 本の規則を追加で持っても、正答数は 1 問も増えない。

セット内の正答数の上限を計算する。4.4.2 章の規則 1 が反転 2 問以上を要求し、規則 2 が大小語を持たない問題を 1 問以上要求するので、大小語を持つ 4 問のうち一致は最大 2 問である。したがって大小語ソルバーの上限は 2 問に、大小語なしの 1 問への対処を足した値になる。

| Lv | 6 章のセットでの値 | 規則下限の最悪構成での値 | 内訳 |
|---|---:|---:|---|
| 4 | 0.60 | 0.60 | 一致 2 問 (例 1、例 3) に加え、大小語なしの 1 問が常に公倍数側になる (N1) |
| 6 | 0.50 | 0.50 | 一致 2 問 (例 1、例 3) と、大小語なしの 1 問を等確率で当てる 0.5 |
| 10 | 0.50 | 0.50 | 一致 2 問 (例 1、例 3) と、大小語なしの 1 問 (例 5 は整列で 0.2 から 0.9) |

実体が片側しか無い象限を 1 問使った構成でも値は動かない。その 1 問は一致問題であり、上の計算の一致 2 問のうちの 1 問を占めるだけだからである。したがってセット内 1 問以下という制限は、この攻撃に対しては掛けなくても同じである。制限自体は、境界が効くかどうかと反転の同値を切るために `scene_gcd_capped` と `scene_lcm_floored` を使う代償として置かれたものなので、残しておいて害は無い。11 章の見積りが「この 1 問を取られる前提で計算している」と書いているのは二重計上であり、見積りは実際より 0.20 だけ保守側にぶれている。

したがって設計側の制限で足りている。ただし Lv4 だけは別の理由で 0.60 になり、doc が 6 章と 4.4.2 章で 3 回書いている「上限は 0.40 である」は Lv4 について成立しない。

#### 3. Lv1 から Lv3 と Lv7 の T2 への運用注の適用

まず Lv7 が v0.2 の 0.55 から v0.3 で 1.00 に戻った理由を特定した。問題文も 5 問構成も v0.2 から変わっていない。変わったのは 9.2 章で、`rem_same` に上限つきの変種、`rem_count` に順位を問う変種を足して normal 3 問の帯を重ねたことである。v0.2 では `rem_same` の与件 3 数がすべて 12 以下、他の 2 種が 50 以上の数を持っていたため、帯を見るだけで 1 問を切り出せた (再監査 H6)。この構造的な経路が閉じた結果、残った判別手段が問い文の語だけになり、語彙ソルバーの値が上がった。T1 を下げると T2 が上がるのは一般に起きることで、v0.3 の Lv7 はその典型である。Lv8 と Lv9 も同じ理由で全語彙ソルバーには 1.00 に近く、設計側が Lv7 だけを運用注の対象として挙げているのは、対象の数え落としである。

判定に使う線引きを明示する。前回 5.2 章で「語彙が学習対象を代替するか、出力を選ぶだけか」と書いた基準を、今回は次の形に具体化した。

- 保護する語彙: 問い文がどの量を問うているかを名指しする語 (約数、倍数、最大公約数、人数、何曜日、何回、個数)、および用語の定義そのもの (わり切れる、あまる、前と後)。これらを読んで正しい量を同定し、そのうえで教えた手続きを当てて正答することは、解いていることであって機械化ではない。
- 保護しない語彙: 問い文が名指しする量が両側で同じであるのに、語と演算が結びついている場合。典型が大小語である。「いちばん大きい 1 辺」と「いちばん小さい 1 辺」はどちらも 1 辺の長さという同じ量を問うており、語は極値の向きを言っているだけで、どちらの向きの計算をするかは場面からしか決まらない。答案の表層形から診断ラベルへの写像も保護しない。ラベルは問い文が名指しする量ではない。

この線引きで 4 段を判定する。

Lv1。分岐に使う語は「ぬけている」「まざっている」「でない」「何個」である。前回 5.2 章では、これらが量名ではないため運用注の文言の保護範囲外だが、どの分岐に入っても約数の集合を作らなければ答えが出ないという理由で適用を認めた。今回の線引きでも同じ結論になる。「ぬけている数」と「まざっている数」は、同じ列に対して異なる量を名指しており、両側で同じ量を問うてはいない。適用する。適用した場合 0.60、適用しない場合 1.00。

Lv2 と Lv3。「倍数」「約数」「最大公約数」「最小公倍数」はいずれも量名そのものである。適用する。適用した場合 0.56 と 0.40、適用しない場合いずれも 1.00。

Lv7。適用する。理由は 2 つある。第一に、normal 3 問の分岐に使う「わり切れる」「あまる」「何個」は定義と量名である。第二に、立式と find_all の分岐に使う「人数を求めるには」は、その問題で未知の量が割る数であることを名指ししている。同じ場面で割られる数を問えば別の量を名指しすることになり、両側で同じ量を問う構造ではない。名指しされた量を読んだあとに 7.1 章のわざ (割る数を聞かれたら、あまりを引いてから約数を考える) を当てる手順は、教えた手続きの適用であって機械化ではない。適用した場合 0.62、適用しない場合 1.00。

ただし Lv7 には条件を付ける。4.5 章が Lv4 から Lv10 に連鎖を義務づけ、3.1 章では余りの領域で公約数側の pattern が `rem_divisor` の 1 つしかない。連鎖の 2 問が同じ `domain` と `direction` を共有する以上、公約数側で連鎖を組むとセット内に `rem_divisor` が 2 問並ぶ。6 章の例示セットがその形である。この 2 問は同じ場面の同じ問いなので、片方を読めば他方も決まる。運用注を適用しない立場を取ると、この 2 問で 0.40 が確定し、normal の残りを足して 0.76 になる。適用の可否がこの段の判定を動かすので、線引きの根拠を上に明示した。連鎖を公倍数側で組めば `rem_divisor` は 1 問に収まるため、この形を推奨として 6.3 に書いた。

Lv8 と Lv9 も同じ扱いになる。Lv8 の「何曜日ですか」と「何回ありますか」は異なる量を名指ししており、しかも曜日は 7 未満という定義から選択肢の値で絞れる。保護する。Lv9 の量名も保護する。どちらも保護しない部分 (答案の表層形から診断ラベルへの写像) が残るので、その分だけを数えて 0.58 と 0.64 になる。

#### 4. v0.3 の新設要素が作った穴

6.4 章に 8 件を列挙した。新設要素との対応だけをここに書く。

出題順の乱数化は穴を作っていない。4.6 章の 3 制約と 4.3 章のペア隣接は同時に満たせる。ただし 6 章の Lv8 だけが固定の出題順を書いており、規則と例示が食い違う (N5)。

`scene_gcd_capped` と `scene_lcm_floored` の新設と境界の導入は、9.2 章の帯に 2 つの隙間を作った。上限 C の帯に下限が無いため、C が最小公倍数未満になる組では公倍数側の問いが解を持たない (N2)。下限 L の帯は正しく書かれているが、6 章の 3 セットがその帯を守っていない (N3)。

四象限規則そのものは有効だが、Lv4 の pattern 集合に公約数側の大小語なし pattern が無いため、規則 2 が要求する 1 問が常に公倍数側になる (N1)。

`cycle_weekday_back` は H5 を閉じており、新しい手がかりを作っていない。「何日前」の答えも曜日名で、与件の形も同じである。

「わけ」欄の衝突表は H4 を閉じており、新しい一意マーカーを作っていない。

Lv6、Lv9、Lv10 の配合組み直しは、Lv10 の配合を回ごとに変える副次効果を持ち、H1 の後半 (位置から pattern を引く経路) への追加の防御になっている。Lv9 は組み直しの結果、場面問題の枠が `scene_lcm_time` 1 種のままで残った (N6)。

部品集合の 4 軸変化は H3 を完全には閉じないという設計側の申告どおりである。監査対象から外したが、Lv5 と Lv10 の値には整列の T2 を 0.7 と置いて計上した。それでも両段は閾値を下回る。

### 6.3 残る修正案

| # | 穴 | 最小修正 |
|---|---|---|
| N1 | Lv4 の大小語なし枠が常に公倍数側 (`scene_lcm_nth` のみ)。Lv4 の大小語ソルバーの上限は 0.40 でなく 0.60 | 4.2 章の Lv4 の pattern 集合に `scene_gcd_nth` を足し、3.1 章の初出 Lv を 6 から 4 へ下げる。あわせて 6 章と 4.4.2 章の「上限は 0.40」の記述を Lv4 について直す |
| N2 | 9.2 章の上限 C が大の帯に下限が無い。C が最小公倍数未満の組では公倍数側が解を持たず、生成器が解無しの問題を出しうる | 帯を「最小公倍数以上、最小公倍数の 6 倍以下、かつ max(a, b) より大きい」に直す |
| N3 | 6 章の 4 か所が 9.2 章の帯に違反する。Lv4 例 3 (10 と 15 に L=5、帯は 2 から 4)、Lv6 例 3 (9 と 12 に L=5、帯は 2)、Lv10 例 3 (8 と 20 に L=5、帯は 2 から 3)、Lv6 例 1 と例 2 (6 と 8 に C=200、帯は 8 より大きく 144 以下)。L の 3 件はいずれも公約数側の読みが空になる組で、答えの存在だけで向きが決まる | 例示の数値を帯の中へ直す。L は 2 または 3、Lv6 の C は 144 以下にする |
| N4 | Lv5 の normal 2 問は「与件の形が完全に一致する」と書かれているが、`factor_count_cond` は条件節に数を 1 つ余分に持つ。式の後ろに現れる数の個数で 2 種が分かれ、検証 7 (与件の数の個数が一致) にも掛かる | 条件を数で書かない形にする (いちばん小さい素数でわり切れるもの)。または `factor_count` 側にも同じ位置に数を持つ句を足す |
| N5 | 6 章の Lv8 が「出題順は例 1、例 2、例 3、例 4、例 5 とする」と固定順を書いており、4.6 章の乱数化と矛盾する | 他の段と同じ「制約を満たす順列から注入 `random` で選ぶ」に直す |
| N6 | Lv9 の場面枠が `scene_lcm_time` 1 種で、場面問題は常に公倍数側。診断 例 4 の向きが読まずに決まる | 4.2 章の Lv9 の pattern 集合に公約数側の場面 pattern を 1 つ足すか、場面問題を Lv9 から外して `common` 領域に閉じる |
| N7 | 立式の選択肢集合はペア内でしか揃わない。反転 B の立式だけ散文肢が 3 つになる (Lv4 例 4 の A、B、C) ため、顔ぶれで反転 B の立式が識別できる | 一致側の立式にも 3 つ目の散文肢 (公倍数をすべて) を置き、散文肢の数を全立式で揃える |
| N8 | 帯の重なりが「30% 以上」の下限規定で、残り 70% では帯が分離したまま (Lv2、Lv7) | 検証 7 の要求を「重なる区間から 60% 以上」に上げるか、帯そのものを完全に重ねる。Lv2 の T1 が 0.56 から 0.45 前後に下がる |
| Lv7 の連鎖 | 公約数側で連鎖を組むと `rem_divisor` が 2 問並び、運用注を適用しない立場では 0.76 になる | 4.2 章の Lv7 に「連鎖は倍数側で組む」を足し、`rem_divisor` をセット内 1 問に保つ |

### 6.4 Phase 4 へ進んでよいかの所見

進んでよい。ただし付録 A を書く前に N3、N4、N5 の 3 件を直すこと。この 3 件は設計の変更ではなく、例示の数値と 1 行の記述の修正である。

判断の根拠を 3 つに分けて書く。

第一に、設計に手を入れないと実在証明が破れる箇所は 3 件で、いずれも修正が局所的である。N3 は 6 章の 4 か所の数値が 9.2 章の帯の外にあり、検証 32 (パラメタ帯) が確実に落とす。N4 は Lv5 の normal 2 問の与件の個数が一致せず、検証 7 が落とす。N5 は Lv8 の出題順が 4.6 章の規則と矛盾し、検証 15 が落とす。付録 A は 6 章の 5 問構成をそのまま制約一覧へ照らす作業なので、この 3 件を残したまま書くと必ず落ちる。逆に言えば、直せば落ちる箇所は他に見当たらない。4.2 章の最低要件、9.4 章の空間サイズと上限、4.3 章のペア条件、4.4.2 章の四象限規則、5.2 章の形式配合は、6 章の 10 セットすべてで同時に満たされている。組合せ破綻は無い。

第二に、残る穴のうち N1、N2、N6、N7、N8 と Lv7 の連鎖は、いずれも規則を 1 行足すか帯を 1 つ動かすかで閉じる。設計の骨格 (境界を全問に持たせる、四象限で写像を壊す、対比ペアを同一形式で組む、出題順を乱数化する) には触れない。値への影響も、最大の N8 で Lv2 の T1 が 0.11 動く程度で、判定区分は変わらない。付録 A と並行して直せる。

第三に、これ以上は doc を読んでも決まらない。Lv5 の 0.71 という最大値は、整列の数ゼロ設計に対する役割語照合ソルバーの強さと、診断の表層形衝突がプールの中でどれだけの割合を占めるかの 2 つに依存する。どちらも生成器を書いて 200 セットを測らないと確定しない。前者は H3 として実装制約が確認済みで、選別形を諦めた以上、doc の側でこれ以上打てる手は無い。後者は 4.8 章と検証 20 と 22 が要求を書いており、実装が要求どおりかどうかの問題である。Lv7 と Lv9 の 0.62 と 0.64 も同様に、実装後の敵ソルバー回帰テスト (検証 37) で測るべき値である。設計側が 12 章の未確定事項に残した 3 項目 (四象限の空象限の扱い、数ゼロ設計の選別、診断の衝突の実装) は、いずれも実測待ちに落ちる性質のもので、Phase 4 の入口で決着させる必要は無い。

あわせて、検証 37 と 38 の書き方は評価する。攻撃の手続きを doc から外し、本監査 doc を攻撃集合の正本に指定したのは、H8 (敵ソルバーの定義が攻撃より弱い) への正しい対処である。今回追加された攻撃は 6.1 章の手続き列と 6.4 章の N1 から N8 で、実装側はこれを回帰テストへ足すことになる。とくに Lv4 の「大小語が無ければ公倍数側」と、Lv5 の「数ゼロ設計の役割語で並べる」と、Lv9 の「場面問題は常に公倍数側」の 3 つは、現行の閾値 (場面 3 段の大小語系 0.70 未満、整列系 0.35 未満、診断のラベル同定系 0.50 未満) で測ると Lv4 と Lv9 が境界に近い。実装時に最初に落ちるならここである。

## 7. 最終監査 (v0.5 対象)

- 対象: `docs/komorebi_seisu_curriculum.md` v0.5 (1969 行、commit `bf7c56f`)
- 範囲: 依頼により v0.4 (`7bcb2d2`) からの差分に絞る。網羅監査は行わない。差分は発案者査読の決定に由来する 5 点 (`correct_alternative` の廃止、`swap_gcd_lcm` の文言変更、3 数の Lv10 追加、反転要件の相対化、doc 整合の修正 2 件)
- 位置づけ: Phase 7 (実装) の直前確認

### 7.0 結論 3 行

1. 4 点の判定はいずれも「耐性は下がっていない」である。ラベル廃止で診断の推測正答率は上がらず (顔ぶれの通り数は Lv8 で 10 から 4、Lv6 で 35 から 20、Lv9 で 20 から 10 へ減ったが、位置がシャッフルされ正答案の比率が 20 から 30% に縛られているため、推測の上限は 0.30 のまま)、3 数の追加も新しい構造盲目の手がかりを作っていない。
2. ただし反転要件の相対化は、意図した Lv10 だけでなく Lv4 と Lv6 も緩めている。`floor(n ÷ 2)` は n 自体に下限が無いため、大小語を持たない問題を増やせば n を下げられる。Lv4 では規則に完全適合したまま反転 1 問の構成が作れ、大小語ソルバーは 0.50 から 0.60 に上がる。Lv6 は大小語なしの pattern が 4 種あるため n = 1 まで下げられ、反転ゼロの構成が規則に適合する。4.4 章の主装置がセット単位で消える。
3. 実装前に直すべき箇所が 3 件ある。いずれも編集で足り、設計への手戻りは不要である。最も重いのは、相対化した反転要件が正本 (4.4.2 章の規則 1) と検証 9 に反映されておらず、両者が絶対値のまま残っていることである。付録 A.10 は自分の検証 9 に落ちる。次が 3 数の 4 pattern が 3.1 章の正本表に無いことで、決定 7 が「3.1 章に足した」と書いているが表に行が無い。v0.4 の P10 と P11 でこの doc 自身が踏んだ sakumon 教訓 2 を、v0.5 でもう一度踏んでいる。

### 7.1 差分の判定

#### 1. ラベル廃止による診断の推測正答率

上がっていない。判定の内訳を 3 つに分ける。

第一に、4 肢の顔ぶれの通り数は減った。4.8 章は 4 肢を「正答肢 1 つと可用な誤りラベルから 3 つ」と定めるので、通り数は誤りラベル数から 3 つを選ぶ組合せになる。v0.4 では `correct_alternative` が誤答肢として使えたため候補が 1 つ多かった。

| Lv | 誤りラベル数 | v0.4 の通り数 | v0.5 の通り数 | 推測正答率 |
|---|---:|---:|---:|---:|
| 5 | 3 | 1 | 1 | 0.25 から 0.30 |
| 6 | 6 | 35 | 20 | 0.25 から 0.30 |
| 8 | 4 | 10 | 4 | 0.25 から 0.30 |
| 9 | 5 | 20 | 10 | 0.25 から 0.30 |
| 10 | 8 | 84 | 56 | 0.25 から 0.30 |

第二に、通り数が減っても推測正答率は上がらない。理由は 2 つある。正解の位置が注入 `random` でシャッフルされる (4.6 章、検証 28) ので、顔ぶれが固定でも位置は読めない。そして顔ぶれと答えの相関が無い。答えが誤りラベルのときは 4 肢が「正しい + 答えのラベル + 他の誤り 2 つ」、答えが `correct` のときは「正しい + 誤り 3 つ」であり、どちらも「正しい 1 つと誤りラベル 3 つ」という同じ形になる。顔ぶれの形からは、答えが正答案かどうかすら読めない。常に「正しい」を選ぶソルバーは、正答案の比率が 20 から 30% に縛られているためその範囲しか取れない。

第三に、Lv5 は縮んでいない。可用ラベルが 3 種ちょうどなので 4 肢は `correct` と 3 つの誤りラベルで固定であり、通り数は 1 である。これは v0.1 監査の時点からの状態で、v0.5 で新たに縮んだわけではない。`correct_alternative` は Lv5 では肢そのものを使わない設計だったため、廃止の影響を受けない。8.1 章の前置き (誤りラベルが 3 種を切ると顔ぶれが固定する) は、3 種ちょうどでも固定するという点で今も不正確だが、上の第二の理由により実害は無い。

v0.5 で実際に消えたのは、v0.4 の検証 24 が持っていた「`correct_alternative` の肢を含む問題のうちその肢が正解である割合が 40% 以下」という上限である。この上限は、肢を見て「正しい (べつのとき方)」を選ぶソルバーに 0.40 まで許していた。廃止によりこの経路は 0.25 から 0.30 の推測に戻る。したがってラベル廃止は診断の耐性をわずかに上げている。監査 S6 の対処が対処ごと不要になるという設計側の説明は正しい。

#### 2. 3 数の追加が作る手がかり

新しい構造盲目の手がかりは見つからなかった。設計側の説明 (与件 3 数で場面問題と個数が一致する) は正しいが、それだけでは足りないので、依頼にあった 2 点を個別に見た。

候補列の作り方。付録 A.10 の P12 ペアは候補 7 個 (1, 2, 3, 6, 9, 72, 144) を 2 問で共有し、その中に公約数 (1, 2, 3, 6) と公倍数 (72, 144) の両方が入る。候補の帯から向きは読めない。これは Lv3 の P8 ペアで確立した形と同じで、9.2 章の共通制約 (誤答候補にもう一方の向きの集合に属する数を 1 個以上含める) も満たす。3 数にしたことで公約数の集合は小さくなり公倍数は大きくなるが、候補列が共有である以上、どちらを選ぶかの判断は候補の見た目から決まらない。

答えの集合の大きさ。A.10 では公約数側が 4 個、公倍数側が 2 個である。この差は答えを出したあとに初めて分かるもので、解答前のソルバーは使えない。集合の大きさで向きを当てる手続きを書こうとすると「候補のうち小さいものを選ぶ」になるが、それは候補の帯を使う手続きと同じで、候補が共有である以上ペアの片方しか取れない。

新しく気づいた点を 1 つだけ挙げる。4.2 章の Lv10 は「場面問題 2 問以上」を要求し、形式は整列 1 問が必ず入る。P12 ペアを引いた回は find_all が 2 問を占めるので、残り 2 問が場面問題に確定する。すなわち 3 数の find_all を見た時点で、そのセットの構成が整列 1 と場面 2 に確定する。Lv10 で配合を毎回変える (5.2 章) という設計の狙いに対しては後退だが、構成が分かっても各問の向きは分からないので、正答率には効かない。P12 を引く確率は形式の抽選が一様なら 4 分の 1 である。

#### 3. `floor(n ÷ 2)` への相対化

Lv4 と Lv6 の反転混入率を下げている。ここが今回の最も重い所見である。

まず正本の状態を確認する。4.4.2 章の規則 1 は v0.5 でも「反転を 2 問以上置き、そのうち 1 問以上を反転 B とする」のままで、絶対値である。検証 9 も「反転が 2 問以上あり」のままである。相対化した形 (`floor(n ÷ 2)` と一致 1 問以上) が書かれているのは 13 章の決定 7 の記述と付録 A.10 の注記だけで、規則にも検証項目にも反映されていない。したがって doc を字義どおり読むと A.10 は規則違反であり、検証 9 に落ちる。

そのうえで、意図された相対化を採用した場合に何が起きるかを計算した。n を「大小語 (`ask` が `max` か `min`) を持つ問題の数」とすると、下限は反転 `floor(n ÷ 2)` 問と一致 1 問である。n には下限が無い。大小語を持たない問題 (`ask` が `count` か `nth`) を増やすほど n は下がり、必要な反転数も下がる。

| Lv | 大小語なしに使える pattern | n の下限 | そのときの反転の下限 | 大小語ソルバーの値 |
|---|---:|---:|---:|---:|
| 4 | `scene_gcd_nth`、`scene_lcm_nth` の 2 種 | 3 | 1 | 0.60 |
| 6 | `scene_gcd_nth`、`scene_lcm_nth`、`scene_gcd_ways`、`scene_lcm_ways` の 4 種 | 1 | 0 | 0.60 |
| 10 | 場面以外の全域 | 2 (場面問題 2 問以上より) | 1 | 0.44 |

Lv4 の 0.60 を作る適合構成を実際に組んで検算した。`scene_gcd_nth` (nth、gcd)、`scene_lcm_nth` (nth、lcm)、`scene_gcd_size` (max、gcd、一致)、`scene_lcm_size` (min、lcm、一致)、`scene_lcm_capped` (max、lcm、反転 A) の 5 問である。gcd 側 2 問、lcm 側 3 問、patternId は 5 種で上限 1 を満たし、(max, 上限が大) には gcd と lcm が 1 問ずつ入って規則 3 を満たす。n = 3 なので反転は `floor(3 ÷ 2)` = 1 問で足り、一致は 2 問ある。大小語ソルバーは一致 2 問を確実に取り、反転 1 問を落とし、大小語なし 2 問を 0.5 ずつ取るので 3.0 問、すなわち 0.60 になる。v0.4 の絶対値ではこの構成は反転 1 問なので違法だった。

Lv6 はさらに緩む。大小語なしの pattern が 4 種あるので、n = 1 の構成が組める。そのとき反転の下限は `floor(1 ÷ 2)` = 0 で、一致 1 問以上だけが残る。反転がゼロのセットが規則に適合し、セット内で大小語から向きへの写像が関数になる。4.4 章が「反転を混ぜないと写像が関数として定義できてしまう」と書いた状態そのものが、規則の下限で復活する。

Lv10 は依頼のとおり n = 2 で反転 1 問と一致 1 問になり、A.10 の値は 0.44 である。ここは相対化の狙いどおりで問題ない。

原因は、相対化のときに n の下限を書かなかったことである。修正は 7.3 に書いた。なお、絶対値で書いた下限が小さい空間で破綻するのは sakumon 教訓 1 の型だと 13 章が自ら書いているが、相対化した下限が母数の操作で骨抜きになるのはその裏返しで、相対化のときは母数の下限も同時に書く必要がある。

#### 4. 新しい doc 内矛盾

削除した規則を参照している箇所は残っていない。`correct_alternative` への言及は 12 か所あるが、すべて改版記録と経緯の説明であり、規則として生きているものは無い。「分ける話とそろえる話」も残り 2 か所はいずれも経緯の説明である。この点は問題ない。

一方で、別種の矛盾が 3 件ある。7.3 に修正案とともに書いた。要点だけ挙げる。

反転要件の正本が更新されていない (7.1 の 3 で述べた)。

3 数の 4 pattern が 3.1 章の正本表に無い。3.1 章の表は 43 行のままで、末尾は `cycle_align_count` である。決定 7 は「3.1 章に `gcd3_num`、`lcm3_num`、`common_div3_select`、`common_mul3_select` の 4 pattern を足し」と書くが、表に行が無い。3.5 章の索引は「43 種」、9.4 章の Lv10 は「47 種から 4 種を除く」、検証 3 は「38 語」(v0.2 期の値)、9.3 章は「28 pattern」(v0.1 期の値)、検証 14 と 35 は「43 pattern」で、5 通りの数が並んでいる。あわせて、3 数の `unknown` の割り当て (9.3 章の 20 種のどれか) と、3 数のパラメタ帯 (9.2 章に a, b, c の行が無い) も未定義である。7.1 章のわざ表に 3 数に対応する行が無いため、検証 35 (全 pattern にわざの行が引ける) も現状では落ちる。

別解答案の規則が Lv5、6、8 で満たせない。4.8 章は「正答案のうち 3 分の 1 以上を別解答案にする」を Lv の区別なく書き、検証 24 も「正答案の比率が Lv ごとのプール全体で 20 から 30% で、そのうち 3 分の 1 以上が別解答案である」と書いたうえで「別解答案を出すのは Lv9 と Lv10 に限る」と続ける。8.1 章は「Lv5、Lv6、Lv8 は手順が一本道で別解が立たない」と書く。Lv5、6、8 のプールでは前半の要求が満たせず、検証 24 は生成器が何をしても落ちる。

### 7.2 全 10 Lv の T1 と T2 の一覧

T2 は運用注の適用後の値 (保護語彙による正答を除いた値) である。線引きは 6.2 章の 3 に定義した。v0.3 列は三次監査で本監査が独立に出した値、v0.5 列は今回の値である。v0.4 で動いた分は、三次監査が指定した修正 (N1 から N8 と Lv7 の連鎖) が適用された結果として本監査が引き継いだもので、v0.5 の差分の外にあるため独立には再導出していない。

| Lv | T1 v0.3 | T1 v0.5 | T2 v0.3 | T2 v0.5 | 変化の出どころ | 判定 |
|---|---:|---:|---:|---:|---|---|
| 1 | 0.60 | 0.60 | 0.60 | 0.60 | 変化なし | 耐性あり |
| 2 | 0.56 | 0.56 | 0.56 | 0.56 | 変化なし。N8 (帯の重なりが 30% 止まり) は未修正のまま残る | 耐性あり |
| 3 | 0.40 | 0.40 | 0.40 | 0.40 | 変化なし | 耐性あり |
| 4 | 0.47 | 0.47 | 0.60 | 0.60 | N1 の修正 (`scene_gcd_nth` の追加と向きの抽選) で 0.50 へ下がったが、反転要件の相対化で 0.60 へ戻る。値は同じでも原因が入れ替わっている | 耐性あり |
| 5 | 0.58 | 0.42 | 0.71 | 0.64 | v0.4 の 2 修正 (normal 2 問の P10 ペア化、数あり整列を非連鎖形に限定)。ただし D2 は素数の昇順で並ぶため整列 1 問目は 0.8 のまま | 耐性あり (最薄) |
| 6 | 0.40 | 0.40 | 0.54 | 0.60 | 反転要件の相対化。n = 1 の構成では反転ゼロが適合する | 耐性あり |
| 7 | 0.62 | 0.62 | 0.62 | 0.56 | v0.4 で `rem_divisor` をセット内 1 問に限り連鎖を倍数側に寄せた (三次監査の推奨) | 耐性あり |
| 8 | 0.45 | 0.45 | 0.58 | 0.58 | 変化なし。ラベル廃止は顔ぶれの通り数を 10 から 4 に減らしたが推測正答率は変わらない | 耐性あり |
| 9 | 0.40 | 0.40 | 0.64 | 0.52 | v0.4 で場面枠を 2 種抽選にした (N6) ぶんと、v0.5 で肢の実在から答えを読む経路が構造的に消えたぶん | 耐性あり |
| 10 | 0.45 | 0.45 | 0.55 | 0.52 | 3 数ペアの回は候補共有で向きが読めず 0.44。他の回を含めた代表値 | 耐性あり |

閾値 0.75 を超える段は無い。0.70 を超える段も無い。最大は Lv5 の 0.64、次いで Lv4 と Lv6 の 0.60 である。Lv4 と Lv6 の 0.60 は規則の下限を突いた最悪構成での値で、付録 A の構成では 0.50 に留まる。

### 7.3 実装前に直すべき箇所

| # | 箇所 | 内容 | 修正 |
|---|---|---|---|
| F1 | 4.4.2 章の規則 1、検証 9 | 相対化が正本に反映されていない。両者とも「反転 2 問以上」の絶対値のままで、付録 A.10 (反転 1 問) が自分の検証に落ちる。さらに `floor(n ÷ 2)` は n に下限が無く、Lv4 で反転 1 問、Lv6 で反転ゼロの適合構成を許す | 規則 1 を「大小語を持つ問題を n 問置く。Lv4 と Lv6 は n を 4 以上、Lv10 は 2 以上とする。そのうち反転を `floor(n ÷ 2)` 問以上、一致を 1 問以上とし、反転の 1 問以上を反転 B とする」に書き換える。検証 9 を同文にする。n の下限は「大小語を持たない問題を Lv4 と Lv6 では 1 問以下」と書いても等価 |
| F2 | 3.1 章の表、3.5 章、9.2 章、9.3 章、7.1 章、検証 3、検証 14、検証 35 | 3 数の 4 pattern が正本表に無い。pattern 総数が 5 通りに割れている (3.1 章 43、3.5 章 43、9.4 章 47、検証 3 の 38、9.3 章の 28) | 3.1 章に 4 行を足して 47 とし、`domain` は common、`direction` は gcd と lcm、与件は「3」と「3 + 候補」、初出 Lv は 10 と書く。3.5 章と検証 3 と検証 14 と検証 35 と 9.3 章の数を 47 に揃える。9.2 章に 3 数の帯 (a, b, c は common と同じ 6 から 60、3 数の最大公約数 2 以上、最小公倍数 4 桁以内) を足す。9.3 章の `unknown` は `gcd`、`lcm`、`commonList` に割り当てられるので 20 種は増やさなくてよい。7.1 章のわざ表に 3 数の行を足すか、既存の方向 2 行が 3 数にも引けることを明記する |
| F3 | 4.8 章、検証 24、8.1 章 | 「正答案のうち 3 分の 1 以上を別解答案にする」と「別解答案を出すのは Lv9 と Lv10 に限る」が Lv5、6、8 で両立しない。検証 24 は生成器が何をしても落ちる | 4.8 章と検証 24 の該当文を「別解が立つ Lv (9、10) では、正答案のうち 3 分の 1 以上を別解答案にする」に限定する |

F1 から F3 はいずれも編集で足りる。設計の骨格 (境界を全問に持たせる、四象限で写像を壊す、対比ペアを同一形式で組む、出題順を乱数化する、正答肢を 1 種にする) には触れない。F2 は分量が最も多いが、pattern を 4 行足して数を 5 か所で揃える作業である。

このほか、三次監査で挙げた N8 (帯の重なりが 30% 止まりで残り 70% は分離したまま) は v0.5 でも未修正である。Lv2 と Lv7 の T1 が 0.10 前後高く出る原因だが、判定区分は動かないので実装をとめる理由にはしない。実装後に検証 7 の実測 (重なる区間から引いた割合) を見てから決めればよい。

### 7.4 実装への申し送り

#### 敵ソルバー回帰テストの攻撃集合

curriculum の検証 37 と 38 が指定するとおり、攻撃の正本は本 doc である。1 章 (v0.1)、5 章 (v0.2)、6 章 (v0.3)、7 章 (v0.5) で見つけた攻撃を統合する。実装はこの表の全件を回帰テストとして持つ。閾値はいずれも 200 セットでの正答率の上限であり、超えたら失敗とする。

| # | 攻撃 | 対象 Lv | 手続き | 閾値 |
|---|---|---|---|---|
| A1 | 列の最大値 | 1 | 数の列を含む normal では列の最大値を答える | 0.70 |
| A2 | 形式内固定手続き | 1、2、3、5、7、9 | 同一形式の n 問すべてに 1 つの手続きを当てる。形式は出題画面から既知とする | 0.70 |
| A3 | 与件の個数と帯 | 2、4、5、7、8 | 与件の数の個数と各位置の数の帯から pattern を判別し、対応する演算を当てる。帯は 9.2 章の値を使う | 0.70 |
| A4 | 大小語 | 4、6、10 | `max` なら最大公約数、`min` なら最小公倍数を答える | 0.70 |
| A5 | 大小語と数の個数 | 4、6、10 | A4 に「与件が 3 数なら 2 数の最小公倍数を求め第 3 の数をこえない最大の倍数を答える」を足す | 0.70 |
| A6 | 大小語の反転 | 4、6、10 | A4 に「与件が 3 数なら写像を反転する」を足す。H2 の攻撃 | 0.70 |
| A7 | 大小語なしは公倍数側 | 4、6、10 | A4 に「大小語が無ければ公倍数側」を足す。N1 の攻撃 | 0.70 |
| A8 | 境界の有効性 | 4、6、10 | 境界が答えを動かすなら反転側、動かさないなら一致側と決める | 0.70 |
| A9 | 解の存在 | 4、6、10 | 公約数側と公倍数側の両方で答えを計算し、解を持つ側を答える。境界が退化した組を突く | 0.70 |
| A10 | 数値連鎖の整列 | 5、10 | 部品の数値連鎖、素数の昇順、問題文に現れる数の出現順で並べる | 0.35 |
| A11 | 部品数からの設計判別 | 5、10 | 部品数と数を含まない部品の位置から 9.5 章の設計を判別し、判別できた設計の並べ方を当てる | 0.35 |
| A12 | 役割語照合の整列 | 5、10 | 数ゼロ設計の部品を役割語 (素数のかけ算、共通、かける、答え) で正順へ対応づける。H3 の攻撃で、閉じきれていないことが既知 | 0.35 |
| A13 | 答案の式からラベル | 5、6、8、9、10 | 答案の式の表層形を 8 章の W 表で引いて `errorType` を決める | 0.50 |
| A14 | 「わけ」欄からラベル | 6、9、10 | 「わけ」欄に大小語の引用があれば `word_cue`、一般則の記述があれば `correct` とする。H4 の攻撃 | 0.50 |
| A15 | 選択肢の文言 | 8、9 | 選択肢集合に特定の文言 (商に 1 をたす、正しい) があればそれを選ぶ | 0.40 |
| A16 | 常に正しい | 5、6、8、9、10 | 診断では常に `correct` の肢を選ぶ。正答案の比率の上限を突く | 0.35 |
| A17 | 答えの型 | 8 | 回答欄が曜日名か数値かで手続きを選ぶ。H5 の攻撃 | 0.70 |
| A18 | 位置から pattern | 全 Lv | セット内の位置から pattern を引く。配合が固定であることを突く。H1 の攻撃 | 0.70 |
| A19 | ペア内の順序 | 全 Lv | 隣接する 2 問の与件が一致していたら、先に手続き X、後に手続き Y を当てる。H1 の攻撃 | 0.70 |
| A20 | 単位から向き | 4、6、10 | 単位が分か秒なら公倍数側と決める。H9 の攻撃 | 0.70 |
| A21 | 候補の帯 | 1、3、9、10 | find_all で候補の最大値が与件の最大値以下なら公約数側、こえていれば公倍数側の集合を選ぶ | 0.70 |
| A22 | 足場から向き | 4、6、7、9 | 足場が名指しする側の手続きを本問に当てる。S7 の攻撃 | 0.70 |
| A23 | 形式から pattern | 5、7、9、10 | 1 問しかない形式枠の pattern を固定と仮定して手続きを当てる。S8 の攻撃 | 0.70 |
| A24 | 3 数の集合の大きさ | 10 | find_all で正解集合が大きいほうを公約数側と仮定する。v0.5 で新設した P12 に対する攻撃 | 0.70 |

閾値の根拠は監査 1.2 章の昇降均衡点 0.70 である。整列系 (A10 から A12) の 0.35 と診断のラベル同定系 (A13、A14) の 0.50 は、その形式が 1 セット 5 問のうち 1 問から 2 問を占めることを踏まえた個別閾値で、curriculum の検証 37 が定める値と同じである。A15 の 0.40 と A16 の 0.35 は、正答案の比率 (20 から 30%) と 4 肢の推測 (0.25) に余裕を足した値である。

#### 測定と運用

- 測定は 200 セットで行う。A10 から A12 は整列問題 200 問、A13 と A14 と A16 は診断問題 200 問で測る。
- 最初に落ちるとすれば Lv5 の A12 である。H3 は doc の側で閉じきれないことが確定しており (発案者の決定 6 で選別形を見送った)、12 章の未確定事項 2 もそう書いている。A12 が 0.35 を超えた場合、doc の修正では下げられないので、Lv5 の整列 2 問のうち数ゼロ設計を 2 問にする、または整列を 1 問に減らして診断を 2 問にする、といった配合側の対処を検討することになる。
- 次に境界に近いのは Lv4 と Lv6 の A4 から A9 の系列である。F1 の修正を入れれば 0.50 に収まるが、入れないまま実装すると 0.60 になり、閾値 0.70 に対する余裕が 0.10 しかない。F1 は実装前に入れること。
- A18 と A19 は生成器の実装方法に直接依存する。4.6 章の出題順の乱数化と 4.3 章のペア内順序の乱数化が実装されていれば、どちらも 0.50 前後に落ちる。実装で乱数化を省くと両方が 1.00 近くまで上がるので、検証 15 (出題順の一様性) と検証 6 (ペア内順序の一様性) を先に通してから測ること。
- 攻撃集合はこれで確定ではない。curriculum の検証 38 が定めるとおり、新しい攻撃が見つかったら本 doc に追記し、実装は追記分を回帰テストへ足す。実装中に見つけた攻撃も同じ扱いとする。

#### 実装可否

進んでよい。F1 から F3 の 3 件を先に直すこと。3 件とも編集で、設計への手戻りは無い。F1 は敵ソルバーの値に直接効くので必ず先に入れる。F2 は 3 数の pattern が定義されていないと生成器が書けないため、実装の前提として必要である。F3 は検証 24 が確実に落ちるための修正である。
