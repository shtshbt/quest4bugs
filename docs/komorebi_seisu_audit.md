# kom_seisu 機械化可能性監査 (2026-08-28)

- 対象: `docs/komorebi_seisu_curriculum.md` v0.1 (整数の性質、kom_seisu)
- 方法: `docs/komorebi_mechanization_audit_2026_08_14.md` 1 章の 2 層ソルバー (T1 構造盲目、T2 語彙ルックアップ)、閾値 0.75、判定 3 区分、横断穴 C1 から C4
- T2 の運用注: `docs/komorebi_categories.md` 記載原則 6 の運用注 (恣意的な対応表にのみ T2 判定を適用する) を 1.2 章で Lv ごとに適用判断した
- 立場: 対象 doc の設計意図の説明は受け取っていない。11 章の自己見積りは反証対象として扱い、doc の記述だけから独立に計算した

## 0. 結論 3 行

1. 10 段すべてが閾値 0.75 を超え、うち 7 段が 0.90 以上である。最も安いのは Lv1 で、`div_of` の答えが「並んだ約数の列の最大値」であるため、1 文字も読まずに 1.00 で通る。11 章の自己見積り (Lv1 は 0.60) はここを見落としている。
2. 4.4 章の主装置は成立していない。場面 3 段 (Lv4、6、10) が実際に使う反転はいずれも `scene_lcm_capped` (反転 A) で、これは問題文に数を 3 つ持つ唯一の pattern であるため、大小語ソルバーに「数が 3 つなら上限つき公倍数」の 1 行を足すだけで無効になる。4.2 章が反転を「1 問以上」としか要求していないことと合わせると、規則に完全適合したまま Lv4 は T2 で 1.00 に達する。
3. 4.7 章の記号依存は数値照合ソルバーを落とさない。9.5 章の部品設計が 2 通りしかなく、数を含まない部品の位置が設計ごとに固定されているため、部品数で設計を見分けて並べるだけで整列は 1.00 になる。検証 21 が要求する「数なし部品の位置が末尾以外で一様」は、9.5 章の 2 設計では原理的に満たせない。

## 1. 判定表

値はすべて 5 問セットに対する期待正答率である。1 問単位の内訳は 1.3 章に置く。T1 と T2 の高いほうで判定した。

| Lv | T1 | T1 ソルバーの手続き | T2 | T2 ソルバーの手続き | 判定 |
|---|---:|---|---:|---|---|
| 1 | 1.00 | find_all は選択肢に載っていない唯一の数の約数を選び、normal は数が 1 つならその約数の個数、数が並んでいれば最大値を答える | 1.00 | 同左 (語彙に分岐する必要がない) | 機械化可能 |
| 2 | 0.80 | find_all は常に倍数側を選び (対比ペアで 1 問落とす)、normal は 40 以上の数があればその数 ÷ 小さい数の商、無ければ 2 数の積 | 1.00 | 「倍数」「約数」の 2 語で手続きを切り替える | 部分的に可能 (T1 による。1.2 章) |
| 3 | 0.80 | normal は常に最小公倍数、find_all は候補の最大値が 2 数の最大値以下なら公約数、超えていれば公倍数 | 1.00 | 「最大公約数」「最小公倍数」「公約数」「公倍数」の 4 語で分岐 | 部分的に可能 (T1 による。1.2 章) |
| 4 | 0.60 | 数が 3 つ現れたら上限以下で最大の公倍数、2 つなら常に最小公倍数 | 1.00 | 数が 3 つなら上限つき公倍数、それ以外は大なら最大公約数、小なら最小公倍数 | 機械化可能 |
| 5 | 0.85 | 整列は部品数で 9.5 章の 2 設計を判別し (5 部品なら数なし部品を先頭にして数値連鎖、4 部品なら問題文の 2 数の出現順に並べ数なし部品を末尾の 1 つ手前)、normal は数の個数で素数計数と (指数 + 1) の積を切り替え、診断は 4 択から 1 つ選ぶ | 0.95 | 同左 + 診断は答案の式の表層形を 8 章 W 表で引く | 機械化可能 |
| 6 | 0.50 | 単位が分か秒なら最小公倍数、それ以外も最小公倍数、診断は 4 択から 1 つ選ぶ | 0.90 | 「次に」「k 回目」は最小公倍数、数が 3 つなら上限つき公倍数、大小語は語の写像、診断は答案の式を W 表で引く | 機械化可能 (診断の扱いに依存。4 章) |
| 7 | 0.80 | find_all は (大きい数 − 小さい数) の約数のうち小さい数より大きいものを選び、formulation は引き算の肢を選び、normal は小さい数の個数と上限値の有無で 3 手続きを切り替える | 1.00 | 同左 + 「わり切れる」「あまる」「何個」で normal を分岐 | 部分的に可能 |
| 8 | 0.85 | formulation は選択肢集合に「商に 1 をたす」形があればそれ、無ければ「あまり」形を選び、normal は数が 1 つなら N ÷ 7 の商 + 1、2 つなら最小公倍数、診断は 4 択から 1 つ選ぶ | 0.95 | 同左 + 診断は答案が商を使っていれば `quotient_remainder` | 機械化可能 |
| 9 | 0.50 | find_all は 2 数の公約数、normal は常に最大公約数、診断は 4 択から 1 つ選ぶ | 0.95 | 選択肢に「正しい (べつのとき方)」があればそれ、無ければ答案の式を W 表で引く。normal は足場と問い文の語で分岐 | 機械化可能 |
| 10 | 0.73 | 整列は 9.5 章の非連鎖 1 設計に固定なので確実に取り、数が 3 つなら上限つき公倍数、find_all は候補の最大値で公約数側か公倍数側かを決め、formulation は常に最小公倍数、診断は 4 択から 1 つ選ぶ | 0.90 | 同左 + 大小語の写像と W 表 | 機械化可能 |

閾値超えは 10 段中 10 段。0.90 以上 (機械化可能) が Lv1、4、5、6、8、9、10 の 7 段、0.75 から 0.89 (部分的に可能) が Lv2、3、7 の 3 段。0.74 以下の段は無い。

### 1.1 doc の自己見積り (11 章) との差

| Lv | doc の T1 | 本監査の T1 | 差の出どころ |
|---|---:|---:|---|
| 1 | 0.60 | 1.00 | `div_of` の答えが列の最大値であることを見落としている。12 章の未確定 2 は `div_of` を分散装置として置いたと書くが、実際には 28 pattern 中もっとも安い |
| 2 | 0.60 | 0.80 | normal 2 問を数の個数と大小で分けられる。数の個数は監査 1.1 章が T1 に許した手がかりである |
| 3 | 0.40 | 0.80 | find_all 2 問が候補の帯で判別できる。公約数は min(a, b) 以下、公倍数は lcm(a, b) 以上なので帯が重ならない |
| 5 | 0.25 | 0.85 | 整列 2 問を 0.25 と見積もっているが、9.5 章の設計が 2 通りしか無いため部品数で判別できる |
| 8 | 0.25 | 0.85 | 対比ペアの 2 問の選択肢集合が異なるため、選択肢の形だけで問い分けが決まる |
| 10 | 0.25 | 0.73 | 整列 1 問が固定設計で無償、`scene_lcm_capped` が数の個数で無償 |

### 1.2 運用注の適用判断

原則 6 の運用注は「恣意的な対応表 (語から演算への暗記が構造理解を代替するもの) にのみ T2 判定を適用する。量名や単位の定義、問い文が名指しする量の読み取りは学習内容そのものであり、それによる高スコアは不合格としない」と読める。適用に迷った箇所を、適用した場合としない場合の両方の数値とともに示す。

| 箇所 | 適用する場合 | 適用しない場合 | 判断 | 理由 |
|---|---:|---:|---|---|
| Lv2 の「倍数」「約数」 | T2 を不問にし、判定は T1 0.80 | T2 1.00 で機械化可能 | 適用する | 「12 の倍数をえらべ」は問い文が名指しする量の読み取りそのもの。5.1 章がこの段を弾層と定義し、向きの判別を求めていない。ただし T1 が 0.80 なので判定は変わらない |
| Lv3 の 4 語 | T2 を不問にし、判定は T1 0.80 | T2 1.00 で機械化可能 | 適用する | 同上。判定は T1 で決まる |
| Lv7 の「わり切れる」「あまる」 | T2 を不問にし、判定は T1 0.80 | T2 1.00 で機械化可能 | 適用する | わり算の定義の読み取りに当たる。ただし本 Lv の学習内容である「割る数を求めるなら引く」を選択肢の形だけで取る手続きは T1 に属し、そちらが 0.80 を作っている |
| `next` と `nth` から最小公倍数への写像 (Lv6、8、10) | 該当箇所を 0.5 に落とすと Lv6 の T2 は 0.80 | Lv6 の T2 は 0.90 | 適用しない | 3.4 章自身が「`next` と `nth` は公倍数側にしか実体が無い」と書いており、語から演算への 1 対 1 写像として暗記できる。4.4 章が `max` と `min` についてだけ写像を壊しにいっていること自体が、`next` と `nth` の写像が生きている証拠である。量名でも単位の定義でもない |
| 診断ラベル (答案の式の表層形から W 表を引く) | 診断を一様推測 0.25 とすると Lv5 0.85、Lv6 0.70、Lv8 0.85、Lv9 0.87、Lv10 0.83 | 上表のとおり Lv5 0.95、Lv6 0.90、Lv8 0.95、Lv9 0.95、Lv10 0.90 | 適用しない | 誤りを型で呼べることは学習内容だが、ソルバーが使うのは「答案に × があれば `not_minimal`」「答案が商を使っていれば `quotient_remainder`」という式の表層形の照合であり、場面の向きを一度も読んでいない。ただしこの判断が判定を動かすのは Lv6 だけである (適用すると Lv6 のみ 0.70 の耐性あり) |

### 1.3 値の内訳

セットは 6 章の Lv 別詳細に書かれた 5 問構成を用い、4.2 章の最低要件だけを満たす最悪構成が別にある場合は併記する。

Lv1 (T1 1.00): 例 1 `div_count` は数が 1 つなので約数を数えて可、例 2 と例 3 相当の `div_of` 2 問は列の最大値を書くだけで可、`div_select` 2 問は選択肢に載っていない数の約数を選ぶだけで可。5 問すべて。足場は `scaffold` が別 field なので `text` だけを読むソルバーの邪魔をしない。

Lv2 (T1 0.80): find_all 3 問は `mul_select` 2 と `div_select` 1 で、常に倍数側を選ぶと 2 問。normal 2 問は `mul_count` (40 以上の数を持つ) と `mul_nth` (小さい数 2 つ) で、数の大小で分けて 2 問。合計 4 問。

Lv3 (T1 0.80): normal 3 問は `gcd_num` 1 と `lcm_num` 2 で、常に最小公倍数を答えると 2 問。find_all 2 問は候補の帯で判別して 2 問。合計 4 問。5 問目の pattern を抽選する実装では 0.70 から 0.80 のあいだ、期待 0.75。

Lv4 (T1 0.60、T2 1.00): T1 は `scene_lcm_capped` を数の個数で取り、`scene_lcm_size` 2 問を常時最小公倍数で取り、gcd 側 2 問を落とす。T2 は 6 章の例示セット (反転 2 問) に対しては 0.80、4.2 章の最低要件だけを満たす最悪構成 (反転 1 問で、それが反転 A) に対しては 1.00。詳細は 2.3 章。

Lv5 (T1 0.85): 整列 2 問はいずれも 9.5 章の設計そのままなので 2 問、normal 2 問 (`prime_check` は数 2 つ、`factor_count` は素因数分解の式を含み数が 3 つ以上) は数の個数で分けて 2 問、診断 1 問は 0.25。合計 4.25 問。

Lv6 (T1 0.50): normal `scene_lcm_time` 1 問と formulation `scene_lcm_capped` 1 問 (正解肢が「最小公倍数」) を常時最小公倍数で取り、normal `scene_gcd_count` 1 問を落とし、診断 2 問は 0.25 ずつ。合計 2.5 問。4.2 章の最低要件は gcd 側 1 問以上なので、その 1 問が診断に当たる構成では非診断 3 問がすべて lcm 側になり、常時最小公倍数ソルバーは 0.70 に届く。

Lv7 (T1 0.80): find_all 1 問と formulation 1 問は確定で取り (2.4 章)、normal 3 問のうち 2 問を数の構成で判別。合計 4 問。3 手続きが完全に分離する実装では 1.00。

Lv8 (T1 0.85): formulation 2 問は選択肢集合の形で確定 (2.4 章)、normal 2 問は数の個数で確定、診断 1 問は 0.25。合計 4.25 問。

Lv9 (T1 0.50): find_all 1 問は確定、normal 2 問は常時最大公約数で 1 問、診断 2 問は 0.25 ずつ。合計 2.5 問。T2 では診断 2 問のうち 1 問が `correct_alternative` と 4.2 章が定めているため、その肢の実在で 1 問が確定する (2.5 章)。

Lv10 (T1 0.73): 整列 1 問と `scene_lcm_capped` 1 問は確定、find_all 1 問は候補の帯で 0.9、formulation 1 問は常時最小公倍数で 0.5、診断 1 問は 0.25。合計 3.65 問。

## 2. 横断穴

### 2.1 C1 から C4 に当てはまるもの

C1 (整列の一本鎖規則がそのままソルバーになる)。対処が宣言されているが成立していない。4.7 章は一意性の根拠を `requires` と `produces` の位相順序に置き、数を含まない部品をちょうど 1 つ混ぜると書く。しかし 9.5 章が与える部品設計は 2 通りしかなく、数なし部品の位置は設計ごとに固定されている (`factorize` では先頭、`gcd_by_factor` では 3 番目)。部品数 (5 か 4) は形式から見えるので、ソルバーは設計を判別してから数値連鎖または問題文との数の照合で並べればよい。さらに `gcd_by_factor` の非連鎖形は、問題文が「48 と 60 の最大公約数を」と 2 数をこの順で名指しし、先頭 2 部品がその順で並ぶ。数値の照合だけで正順が決まる。Lv10 は 9.5 章により非連鎖形 1 設計に固定されるため、整列枠の patternId は定数である。

C2 (正解選択肢の位置分散が無規定)。選択肢については塞がれている。4.6 章が注入 `random` でのシャッフルを明記し、検証 22 が位置分布の一様性 (各位置 20 から 30%) を要求している。ただし残余が 2 つある。第一に、整列の部品の初期提示順に規定が無い。検証 20 と 21 は位相順序と数なし部品の位置を検査するが、提示順が正順の固定置換になっていないことは誰も検査しない。第二に、find_all の候補の並び順に規定が無い (6 章の例はすべて昇順)。

C3 (一意性のための固定語彙が lookup table になる)。3 か所で開いている。第一に、3.4 章の `next` と `nth` は公倍数側にしか実体が無いと doc 自身が書いており、語から演算への 1 対 1 写像として完全に働く。第二に、3.3 章の T4 (バス、電車、ライト) は公倍数側専用で、単位が分か秒であることから向きが決まる。単位記号の見た目は監査 1.1 章が T1 に許した手がかりであり、これは T2 ですらない。第三に、8 章の W 表と検証 16 (「`errorType` の誤りが答案に実在し、他の誤りが混入していない」) が、答案の式の表層形から誤りラベルへの一意写像を要求している。品質を保証するために置いた規則が、そのままラベル同定の手続きになっている。

C4 (セット内の型分散規則が無い)。4 本の中で最もよく塞がれている。4.2 章が全 10 段の配合列を持ち、9.4 章が空間サイズを固定し、検証 5 と 10 と 11 が突き合わせる。指摘は 2 点だけである。第一に、配合が Lv ごとに固定で公開されているため、ソルバーは事前にセットの型構成を知っている (Lv1 は `div_count` 1、`div_of` 2、`div_select` 2 の固定配分と書かれている)。第二に、分散を形式ごとに割り当てると形式が型を漏らす。2.6 章に分けて書く。

### 2.2 カテゴリ固有の穴 (概要)

| # | 穴 | 影響する Lv |
|---|---|---|
| S1 | 反転 A (`scene_lcm_capped`) が 3 数を持つ唯一の pattern であり、読まずに識別できるので反転として働かない | 4、6、10 |
| S2 | 4.2 章の「大小語の反転 1 問以上」では検証 30 (大小語ソルバー 0.70 未満) を満たせない | 4、6、10 |
| S3 | `div_of` の答えが列の最大値である | 1 |
| S4 | 対比ペアが数の多重集合の一致しか要求せず、選択肢集合と答えの型の一致を要求していない | 8、9 |
| S5 | 診断は答案の式の表層形から一意にラベルが決まるよう検証 16 が要求している | 5、6、8、9、10 |
| S6 | 4.2 章が Lv9 の診断 2 問のうち 1 問を `correct_alternative` と定めるため、その肢の実在が答えを教える | 9 |
| S7 | 足場が向きを名指しする実例のとき、本文を読まずに向きが決まる | 7、9 |
| S8 | 形式が Lv ごとに固定で UI から既知なので、形式が pattern を漏らす | 5、7、9、10 |
| S9 | 6 章の例示セットが doc 自身の規則に違反しており、実在証明が既に破れている | 6、7 |
| S10 | 検証 21 (数なし部品の位置が一様) が 9.5 章の 2 設計では原理的に満たせない | 5、10 |

### 2.3 S1 と S2 の詳細 (4.4 章の主張の検査)

4.4 章は、問い語と向きの対応が一致する問題と反転する問題を各 1 問以上含めることで、セット内で大小語から向きへの写像が関数として定義できなくなると主張する。混入率を 4.2 章の配合列から読むと次のようになる。

| Lv | 4.2 章の要求 | 6 章の例示セットでの反転数 | 大小語を持つ問題数 | 純粋な大小語ソルバーの値 |
|---|---|---:|---:|---:|
| 4 | 大小語の反転 1 問以上 | 2 (例 4 が反転 B、例 5 が反転 A) | 5 | 0.60 (例示セット) / 0.80 (規則下限) |
| 6 | 大小語の反転 1 問以上 | 1 (例 5 が反転 A) | 2 | 0.50 (残り 3 問は別手続きが要る) |
| 10 | 大小語の反転 1 問以上 | 1 (例 3 が反転 A) | 2 | 0.60 (同上) |

規則の下限は反転 1 問である。5 問すべてが大小語を持つ Lv4 では、反転 1 問の構成に対して大小語ソルバーは 4 問正答し 0.80 となる。閾値 0.75 を超えるだけでなく、doc 自身が検証 30 で設定した基準 (Lv4、6、10 の 200 セットに対し 0.70 未満) も満たせない。4.2 章の配合列と検証 30 が同時に成立しない。規則を満たした生成器が自分の test に落ちる状態である。

さらに重い問題は反転の実体である。反転 A は `scene_lcm_capped` で実現されており、この pattern は 9.2 章により上限 C を持つ。したがって問題文に現れる数が 3 つになる。Lv4、6、10 の他のどの pattern も 2 数である。数の個数は監査 1.1 章が T1 に明示的に許した手がかりであるから、ソルバーは「数が 3 つなら 2 数の最小公倍数を求め、上限以下で最大の倍数を答える」という 1 行を持てばよい。この 1 行は問題文を一切読まない。したがって反転 A は大小語ソルバーに対する反例として機能しない。

6 章の例示セットに対して、この 1 行を足した大小語ソルバーを走らせると次になる。

- Lv4: 例 1 (max 一致) 正、例 2 (min 一致) 正、例 3 (min 一致) 正、例 4 (min 反転 B) 誤、例 5 (max 反転 A) 正 = 0.80
- Lv6: 例 3 (max 一致) 正、例 5 (max 反転 A) 正、例 2 (next) 正、診断 2 問は 0.25 ずつ = 0.70。診断に W 表を使うと 1.00
- Lv10: 例 2 (min 一致) 正、例 3 (max 反転 A) 正、例 1 (count、候補の帯で公約数側と判別) 正、例 4 (整列、固定設計) 正、例 5 (診断) 0.25 = 0.85

規則下限の構成、すなわち反転を 1 問だけ置き、それを反転 A にした Lv4 のセットでは、この解法は 5 問すべてを取る。例として gcd 側に `scene_gcd_size` (max 一致) と `scene_gcd_count` (max 一致)、lcm 側に `scene_lcm_size` (min 一致) 2 問と `scene_lcm_capped` (max 反転 A) を置く構成は、4.2 章の gcd 側 2 問以上、lcm 側 2 問以上、対比ペア 1 組、反転 1 問以上、9.4 章の上限 2 をすべて満たし、検証 7 (一致と反転が各 1 問以上) も満たす。T2 は 1.00 である。

結論として、4.4 章の主張は成立していない。真に写像を壊しているのは反転 B (`scene_gcd_count` の `ask: min`) だけであり、それを義務づける行が 4.2 章に無い。

### 2.4 S4 の詳細 (対比ペアが選択肢と答えの型で割れる)

4.3 章の対比ペアの定義は「問題文に現れる数がすべて同一で、問いの種類または向きだけが異なり、かつ答えが異なる 2 問の組」である。要求しているのは数の多重集合の一致と答えの相異だけで、選択肢集合の一致も答えの型の一致も要求していない。

Lv8 の P5 ペア (6 章の例 1 と例 2) で破れる。例 1 の選択肢は「100 ÷ 7 のあまり」「100 ÷ 7 の商」「100 − 7」「100 × 7」、例 2 は「100 ÷ 7 の商に 1 をたす」「100 ÷ 7 の商」「100 ÷ 7 のあまり」「100 ÷ 7 の商とあまりをたす」である。集合が異なるので、「選択肢集合に商に 1 をたす形があればそれを選び、無ければあまりの形を選ぶ」だけで両方取れる。選択肢の形状は監査 1.1 章が T1 に許した手がかりである。

ペアが normal どうしになった場合も同じ結論になる。`cycle_weekday` の答えは曜日名、`cycle_count` の答えは個数であり、答えの型が違う。問題を読まなくても、答えの入力欄が曜日の選択か数値かで問い分けが決まる。

Lv4 の P3 ペア (例 1 と例 2) は選択肢の顔ぶれまで同一に作られており、この穴は無い。ペアの品質が型によって揃っていない。

Lv7 の formulation にも同種の穴がある。6 章の例 3 の選択肢は 35 − 3、35 + 3、35、35 × 3 で、演算子がすべて異なる。「引き算の肢を選ぶ」だけで正答する。これは検証 15 (誤答の少なくとも 1 つは正解と同じ演算子の並びを持つ) にも違反しており、doc の例題が doc の検証項目を満たしていない。

### 2.5 S5 と S6 の詳細 (診断ラベル)

Lv ごとの可用ラベルの組と、そこから作れる 3 肢の組み合わせ数を数え直した。

| Lv | 誤りラベル数 | 正答肢 | 4 肢の顔ぶれの通り数 (C(誤りラベル数, 3)) | 一様推測 |
|---|---:|---|---:|---:|
| 5 | 3 | `correct` | 1 | 0.25 |
| 6 | 5 | `correct` | 10 | 0.25 |
| 8 | 4 | `correct` | 4 | 0.25 |
| 9 | 5 | `correct` または `correct_alternative` | 10 | 0.25 |
| 10 | 8 | `correct` または `correct_alternative` | 56 | 0.25 |

Lv5 は可用ラベルがちょうど 3 種であるため、4 肢の顔ぶれが完全に固定される。8.1 章は「誤りラベルが 3 種を切ると顔ぶれが固定して選択肢から正解が読めてしまう」と書くが、3 種ちょうどでも顔ぶれは固定する。ただし 4.6 章のシャッフルにより位置は読めないため、顔ぶれの固定それ自体は 0.25 を超えさせない。

ラベル名からの推測は 3 か所で成立する。

第一に `word_cue` (大小のことばにつられている) は、8 章が「`swap_gcd_lcm` との区別は答案に添えた理由による」と定めているのに対し、6 章のすべての答案は「しき」と「こたえ」しか持たず理由を書いていない。この書式のままでは `word_cue` は正解になりえず、常に誤答肢である。除外できると Lv6 と Lv9 の推測は 0.25 から 0.33 に上がる。

第二に `calc_only` (計算だけまちがえている) も 6 章のどの例でも正解になっていない。正解になる答案 (向きも手順も正しくわり算だけを誤る) を生成する規定は 4.8 章にも 9 章にも無い。実際に使われないなら、Lv5 の実効的な答えの空間は `correct`、`count_off`、`factor_incomplete` の 3 つに縮み、推測は 0.33 になる。

第三が最も重い。4.2 章の Lv9 の最低要件は「診断 2 問の `errorType` が互いに異なり、うち 1 問は `correct_alternative`」である。毎セット必ず 1 問の答えが `correct_alternative` だと決まっている。4.8 章は「正答肢は `correct` または `correct_alternative` のどちらか一方」と書くだけで、答えが誤りラベルである問題の正答肢としてどちらを置くかを規定していない。6 章の Lv9 の例では、答えが `correct_alternative` の問題 (例 2) だけが「正しい (べつのとき方)」を持ち、もう一方 (例 4) は「正しい」を持つ。したがって「選択肢に正しい (べつのとき方) があればそれを選ぶ」で 1 問が確定する。残り 1 問も「正しい」を除いた 3 肢からの選択に縮む。診断 2 問の期待は 0.25 ずつの 0.5 から 1.33 に上がる。

正解ラベルの分布については、4.8 章が正答案の比率を Lv ごとのプール全体で 20 から 30% と定めるだけで、誤りラベル間の分布に規定が無い。特定のラベルに寄った実装 (例えば Lv5 が `count_off` に寄る) は検証項目のどれにも引っかからない。この点は 4 章に限界として書く。

### 2.6 S7 と S8 の詳細

足場の向き漏洩 (S7)。4.1 章は足場を「小さい数の実例」に統一すると書くが、実例が向きに中立であることを要求していない。6 章の Lv7 の例 1 の足場は「5 でも 7 でもわり切れるいちばん小さい整数は 35 です」で、公倍数側の手続きを名指ししている。Lv9 の例 1 の足場は「12 と 18 の公約数は 1, 2, 3, 6 です」で、公約数側を名指ししている。どちらも本問の向きと一致するため、足場だけを読めば向きが決まる。4.1 章の Lv4 の例 (公約数と公倍数を両方書く足場) はこの穴を持たない。足場は各セットの前半 2 問に必ず付く (検証 24) ので、漏洩は毎セット 2 問まで及ぶ。

形式が型を漏らす (S8)。5.2 章は形式配合を Lv ごとに固定し、形式は UI から既知である。その結果、形式から pattern への写像が単射に近い段がある。Lv7 の find_all は 1 問だけで、6 章では `rem_divisor` に割り当てられている。Lv9 の find_all も 1 問で `common_div_select` である。Lv10 の ordering は 9.5 章により `gcd_by_factor` の非連鎖形に固定である。Lv5 の diagnosis は 1 問で `factor_count` である。9.4 章の空間サイズは形式配合を無視して pattern を数えているため、形式枠ごとの実効空間は表の値より小さい。C4 対策として置いた型分散が、形式単位では分散になっていない。

### 2.7 S9 の詳細 (doc 内の自己矛盾)

監査の対象外だが、実在証明 (Phase 4) の前に落ちる箇所なので列挙する。

1. 4.2 章の Lv6 は「T4 は 2 問以上使わない」と定め、検証 8 も同文である。しかし 6 章の Lv6 の例示セットは例 1 (バス)、例 2 (電車)、例 4 (ライト) の 3 問が T4 である。
2. 検証 21 は「全生成にわたるその部品の位置が末尾以外で一様」を要求する。9.5 章の部品設計は 2 通りで、数なし部品の位置は `factorize` で先頭、`gcd_by_factor` で 3 番目に固定される。4 部品と 5 部品にまたがる位置分布が一様になることはない。4.7 章の「その部品の位置は注入 `random` により決める」と、同章の「位相順序がちょうど 1 通り」も両立しない。位相順序が一意なら位置は決まっており、乱数の余地が無い。
3. 検証 15 は formulation の誤答の少なくとも 1 つが正解と同じ演算子の並びを持つことを要求するが、6 章の Lv7 の例 3 は 4 肢の演算子がすべて異なる。
4. 9.2 章は remainder 領域の p と q を互いに素と定めるが、6 章の Lv7 の例 2 は 4 と 6 を使っている。
5. 9.2 章は find_all の誤答候補に「もう一方の向きの集合に属する数」を 1 個以上含めると定めるが、6 章の Lv1 の例 3 の解説は「候補の中に 24 の倍数が無い」と自ら書いている。Lv3 の例 4 (6 と 8 の公倍数を選ぶ問題) の候補にも公約数は含まれていない。

### 2.8 patternId 空間と上限規則の突き合わせ (検証 5 の再計算)

4.2 章の上限 `ceil(5 ÷ 空間サイズ)` について、鳩の巣原理で破れる Lv は無い。9.4 章の主張は正しい。各段を計算し直した結果を示す。

| Lv | 空間 | 上限 | 4.2 章の最低要件が要求する最小の型数 | 破れるか |
|---|---:|---:|---|---|
| 1 | 3 | 2 | 固定配分 (2, 2, 1) で最大値 2 | 破れない (余地ゼロ) |
| 2 | 4 | 2 | 4 種すべて。5 問なので 1 種が 2 回 | 破れない |
| 3 | 4 | 2 | 4 種すべて | 破れない |
| 4 | 4 | 2 | gcd 側 2 種以上と lcm 側 2 種以上 | 破れない |
| 5 | 4 | 2 | 4 種すべて | 破れない |
| 6 | 7 | 1 | 5 問すべて相異 (連鎖の 2 問が同型なら 4 種) | 破れない |
| 7 | 4 | 2 | 約数側 1 問以上と倍数側 1 問以上 | 破れない |
| 8 | 4 | 2 | ペア 2 種と `scene_lcm_time` | 破れない |
| 9 | 4 | 2 | ペア 2 種 | 破れない |
| 10 | 26 | 1 | 5 問すべて相異 | 破れない |

ただし 2 点を付す。第一に Lv1 は固定配分の最大値が上限とちょうど一致しており、余地がゼロである。配分を動かす提案 (12 章の未確定 2) は上限規則と同時に検討する必要がある。第二に Lv10 の空間 26 は形式配合と両立しない。5 枠のうち ordering 枠は 9.5 章により `gcd_by_factor` の 1 種に固定されるので、空間 26 が効くのは残り 4 枠だけである。検証 11 (各 Lv で生成しうる patternId の異なり数が空間サイズと一致する) は、ordering 枠が定数である以上、残り 4 枠で 26 種を回すことによってしか満たせない。

隣接相異 (4.2 章) についても破れは無い。Lv1 の (2, 2, 1) は ABABC で満たせる。

## 3. 最小修正案

各行の前半が閾値を下回らせる最小の手、後半が 1 章の弱点 (場面から向きを数量関係で判断する) まで満たす案である。前半だけでは目的を満たさない場合がある。

| Lv | 最小修正 (閾値を下回らせる) | 目的まで満たす案 |
|---|---|---|
| 1 | `div_of` を「約数の列から 1 つだけ欠けている数を答える」形に変え、`div_select` の候補に N の倍数を必ず 1 個入れて N を最大値でなくする | Lv2 の P1 ペア (同一 N、同一候補で約数側と倍数側) を Lv1 の find_all 2 問に前倒しし、5 問中 2 問を単一手続きで取れなくする |
| 2 | `mul_nth` の問題文に範囲の見た目を与えて (1 から数えて k 番目) `mul_count` との数の個数の差を消す | normal 2 問も対比ペアにし、同一の数で個数と順番を問い分ける。find_all のペアと合わせてセット内に 2 組置く |
| 3 | find_all 2 問を同一候補、同一 2 数の対比ペアにし、候補に公約数と公倍数を混在させて帯による判別を潰す | 上記に加え、5 問目の pattern を `gcd_num` か `lcm_num` に固定せず、normal の対比ペアと find_all の対比ペアの 2 組体制にする |
| 4 | 4.2 章の要件を「大小語の反転 2 問以上、うち 1 問は反転 B (`scene_gcd_count` の `ask: min`)」に変える。これで大小語ソルバーは 0.60 に落ちる | あわせて数の個数による反転 A の識別を潰す。上限 C を数として書かず場面の語で与える (1 辺が 1m の板におさまる)、または他の pattern にも第 3 の数を持たせて 3 数を反転 A の signature でなくする |
| 5 | 9.5 章の部品設計を 4 通り以上に増やし、数なし部品の位置を設計間で変える。あわせて問題文の 2 数の出現順と部品の順を独立に抽選する | 整列 2 問のうち 1 問を、部品に数を一切含まない形にする (johou_seiri Lv7 が 5 本中最良の耐性を示した形) |
| 6 | 診断 2 問のうち 1 問を、答案の式の表層形が同じで場面の向きだけが違う 2 通りから抽選する (同じ最大公約数の答案が gcd 場面では `correct`、lcm 場面では `swap_gcd_lcm` になる) | あわせて T4 を実際に 1 問以下に制限し (6 章の例示セットを直す)、単位が分か秒であることから向きが決まる経路を塞ぐ |
| 7 | formulation の誤答 3 つを正解と同じ演算子で作る (35 − 3、35 − 1、32 − 3 のような形) | 足場を両向き併記に変え (公倍数の実例と公約数の実例を並べる)、find_all を `rem_divisor` 以外の pattern でも作れるようにして形式から型への写像を単射でなくする |
| 8 | 対比ペアの 2 問の選択肢集合を完全に一致させる (4 肢を あまり、商、商に 1 をたす、商とあまりをたす で共通化する) | 4.3 章の定義に「選択肢集合と答えの型も一致させる」を追加し、P5 以外のペア型にも適用する |
| 9 | `correct` と `correct_alternative` のどちらを正答肢に置くかを注入 `random` で決め、答えが誤りラベルの問題でも `correct_alternative` を誤答肢に使う | あわせて normal 2 問の `gcd_num` と `lcm_num` を、足場が向きを名指ししない形にする |
| 10 | 9.5 章に非連鎖の部品設計をもう 1 つ以上足し、Lv10 の ordering を 2 設計以上から抽選する | 反転を反転 B に替えるか、Lv4 と同じく反転 2 問以上を要求する |

横断の修正としては次の 3 点が Lv 別の修正より先である。

1. 4.2 章の反転要件を「2 問以上」にし、少なくとも 1 問を反転 B とする。検証 30 の基準 (0.70 未満) と規則を同時に成立させる。
2. 検証 32 (数値照合整列ソルバー 0.35 未満) の敵ソルバーの定義に、部品数から設計を判別する前処理と、問題文に現れる数との照合を加える。現在の定義 (部品に現れる数を照合して鎖を作り、鎖に載らない部品の位置を等確率で選ぶ) は 9.5 章の 2 設計を落とせない。
3. 検証 30 の大小語ソルバーの定義に、数の個数による前処理を加える。現在の定義では反転 A を無償で取る解法を検出できない。

## 4. 監査の限界

doc だけでは判定できなかった項目を挙げる。

1. 診断の誤りラベルの出現分布。4.8 章は正答案の比率 (20 から 30%) だけを定め、誤りラベル間の分布を定めていない。一様なら診断は 0.25、偏っていればそれ以上になる。本監査は一様と仮定した。この仮定は Lv5、6、8、9、10 の値に効く。
2. 答案の式の表層形から誤りラベルを引く解法を T2 として数えるかどうか。1.2 章のとおり数えたが、数えない場合は Lv6 が 0.70 の耐性ありに下がる。10 段のうち判定が動くのはこの 1 段だけである。
3. 整列の部品の初期提示順。doc に規定が無い。正順の固定置換で提示する実装なら整列は無条件に 1.00、真にシャッフルするなら 1 章の値になる。
4. 6 章の例示セットと 4.2 章の規則下限の乖離。生成器がどちらに寄るかで値が 0.2 程度動く。本監査は Lv4 の T2 だけ規則下限を主値とし (2.3 章で最悪構成を明示できたため)、他は例示セットを主値とした。
5. 問い回しの表。7.2 章の 5 は「言い回しの表は開いた集合とし、生成器の内部データに置く」と書き、doc には現行分しか示されていない。C3 の残存量 (語彙表がどれだけ小さいか) を測れない。
6. 形式が UI から既知であるという前提の強さ。監査 1.1 章は形式を T1 の手がかりに含めているが、出題画面が形式のどこまでを事前に見せるかは `komorebi/app.js` の話であり、doc の範囲外である。本監査は形式と答えの入力型 (数値か曜日か集合か) が見えるものとして計算した。
7. `calc_only` と `word_cue` が実際に正解ラベルとして生成されるか。6 章のどの例でも正解になっていないが、生成しないと定めた行も無い。生成されなければ診断の実効空間は 1 段小さくなる。
8. Lv7 の normal 3 問の判別可能性。9.2 章のパラメタ帯からは 3 手続きが分離しうると読めるが、問題文の書き方 (「2 けた」のような語が数を持ち込むか) が確定しないため、2 問分離を主値とし 3 問分離を上限とした。T1 は 0.80 から 1.00 の幅を持つ。
9. 実子の誤答分布。本監査は正答率の上限を計算しただけで、装置が学習効果を持つかどうかは測っていない。
