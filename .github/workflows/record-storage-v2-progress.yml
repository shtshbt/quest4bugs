name: Record storage v2 progress

on:
  push:
    branches:
      - agent/storage-v2-shadow

permissions:
  contents: write

jobs:
  record:
    if: contains(github.event.head_commit.message, '[record-storage-v2-progress]')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout migration branch
        uses: actions/checkout@v4
        with:
          ref: agent/storage-v2-shadow
          fetch-depth: 1

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Update authoritative progress tracker
        run: node tools/record_storage_v2_phase1_progress.js

      - name: Validate document diff
        shell: bash
        run: |
          set -euo pipefail
          git diff --check
          git diff -- .claude_plan/storage_v2_shadow_migration.md

      - name: Commit progress update
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .claude_plan/storage_v2_shadow_migration.md
          git diff --cached --quiet && { echo "No progress changes to commit" >&2; exit 1; }
          git commit -m "record storage v2 Phase 1 integration progress"
          git push origin HEAD:agent/storage-v2-shadow
